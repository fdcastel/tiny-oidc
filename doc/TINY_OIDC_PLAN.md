# Tiny OIDC — Implementation Plan (living document)

| | |
|---|---|
| **Source of truth for behavior** | [TINY_OIDC_SPEC.md](TINY_OIDC_SPEC.md) (283 requirement ids). This plan says *when* and *in what order*; the spec says *what*. |
| **Last updated** | 2026-09-22 |
| **Current focus** | `P4-08` (Google verified on staging over three passes; Microsoft waits for the Entra registration — [issue #2](https://github.com/fdcastel/tiny-oidc/issues/2)); `P7-06` (the release path rehearsed on staging and rebuilt as a staged rollout, ADR 0018; the production steps wait on OP-02, on hold); `P7-08` waits on production; `OP-05` (defaults) and `OP-07` (the 1M benchmark) are the owner's calls. |
| **Branch model** | Direct commits to `main`; every push runs the full gate set. `production` branch is fast-forwarded for releases (spec §12.3). |

## How to keep this plan updated

These rules bind whoever works on the repository, human or agent. The plan is only useful while it is true.

1. **Before starting a task**, set its Status to `🔧 IN PROGRESS` and update *Current focus* above. Work on at most two tasks at once.
2. **A task is `✅ DONE` only when** its code is merged on `main`, every test named in the task passes in CI, coverage is still 100%, and `pnpm trace` shows the task's spec ids as covered (spec §13.12). "Implemented but untested" is `🔧 IN PROGRESS`, never done.
3. **Fill the Commit column** with the short hash (7 characters) of the commit that completed the task. When the task and this plan change in the same commit, the hash is not known yet: write `pending` and replace it in the next commit that touches this file (`git log --oneline -- <path>` finds it).
4. **Never delete or renumber a row.** A task that becomes unnecessary gets `⏯️ DEFERRED` with the reason in Notes, or is superseded by a new row that says "supersedes Pn-mm".
5. **Add tasks with the next free id in their phase** (`P3-12` after `P3-11`). Cross-phase or operator work goes in the *Operator tasks* or *Backlog* tables.
6. **`⏯️ DEFERRED` rows must name the task or decision they wait for** in Notes, so the blocker is visible.
7. **When a spec change alters a task**, update the task text and its Spec column in the same commit as the spec change, and record it in the *Plan change log* at the end of this file.
8. **Update *Last updated*** and the change log on every edit. Keep phases in order; do not start Phase n+1 tasks that depend on an unmet exit criterion of Phase n (independent tasks may run early and say so in Notes).
9. **Operator tasks** (rows `OP-nn`) are for the repository owner. Set them `🔧 IN PROGRESS` when the request has been sent to the owner and `✅ DONE` when confirmed.

### Status legend

| Status | Meaning |
|---|---|
| ✅ DONE | Fix implemented and tested |
| 🔧 IN PROGRESS | Partially implemented or underway |
| ❌ OPEN | Not yet addressed |
| ⏯️ DEFERRED | Delayed or put on hold until another task is finished |

### Column meanings

| Column | Meaning |
|---|---|
| **ID** | `Pn-mm` for phase *n* task *mm*; `S-mm` specification work; `OP-mm` operator tasks; `B-mm` backlog. |
| **Task** | What to build, in actionable terms. Paths are repository-relative. |
| **Spec** | Requirement ids or sections in TINY_OIDC_SPEC.md that the task implements. `pnpm trace` will show them covered when the task is done. |
| **Status** | One of the four values above. |
| **Commit** | Short git hash that completed the task, `pending`, or `—` when not applicable. |
| **Notes** | Blockers, decisions, deviations. |

---

## Phase S — Specification (complete)

| ID | Task | Spec | Status | Commit | Notes |
|---|---|---|---|---|---|
| S-01 | Write the v1 specification from the initial draft; verify Cloudflare limits and tooling facts | all | ✅ DONE | 79fc01c | 268 ids at that point |
| S-02 | Deployment model (Workers Builds, host-neutral repo, Deploy button, bundled reference login app), README, MIT license | §12, §7.9 | ✅ DONE | 17d7620 | |
| S-03 | Reference review against tinyauth and authenti-kate: 12 improvements, 8 simplifications, decision log 28–36 | Appendix B | ✅ DONE | becd7c5 | 283 ids, 6 withdrawn |
| S-04 | This implementation plan | §14.2 | ✅ DONE | 058caee | |

---

## Phase 0 — Foundation (complete)

**Goal:** every CI gate exists and is green on a nearly empty `src/`, so the gates tighten as phases complete instead of being bolted on at the end.

**Exit criteria:** `pnpm lint`, `pnpm typecheck`, `pnpm test` (unit + workers), `pnpm test:e2e` (one smoke test), `pnpm trace` (with the phase allow-list), bundle, config, neutrality and drift checks all pass in `pr.yml`; `wrangler dev` serves `/api/v1/health`.

| ID | Task | Spec | Status | Commit | Notes |
|---|---|---|---|---|---|
| P0-01 | Repository scaffold: `package.json` (scripts `dev`, `build`=typecheck, `test`, `test:unit`, `test:workers`, `test:e2e`, `deploy`, `trace`, `lint`, `gen:openapi`, `gen:config`), `.nvmrc` (24), `pnpm-lock.yaml`, `tsconfig.json` (`strict`, `exactOptionalPropertyTypes`, `noUncheckedIndexedAccess`, `noImplicitOverride`), `biome.json`, host-neutral `wrangler.jsonc` with top-level profile and `env.staging`/`env.production` | §12.1, §14.1, TIO-CFG-001, TIO-GEN-003 | ✅ DONE | 9acb15c | Runtime deps limited to hono, @hono/zod-openapi, zod, jose, @simplewebauthn/server, uuidv7; `jose` and `@simplewebauthn/server` are added when first used (Phase 1, Phase 2) because `knip` fails on unused dependencies. Scripts run on Node 24 type stripping (no `tsx`), so imports carry `.ts` extensions everywhere |
| P0-02 | Test toolchain: Vitest 4 with `vitest.unit.config.ts` (Node) and `vitest.workers.config.ts` (`@cloudflare/vitest-plugin`, Istanbul coverage, thresholds 100/100/100/100 on `src/**`, `src/generated/**` excluded); `playwright.config.ts` with Chromium, Firefox, WebKit projects; `test/support/reset.ts` for per-file storage isolation | TIO-TEST-002, TIO-TEST-003, TIO-TEST-004 | ✅ DONE | 9acb15c | Per-file isolation only; `resetStorage()` in `beforeEach` where needed. Root `vitest.config.ts` runs both configs as projects so coverage is one report; Vitest pinned to 4.1 because `@cloudflare/vitest-plugin` 1.1 requires `^4.1` |
| P0-03 | CI: `.github/workflows/pr.yml` running lint, typecheck, knip, unit, workers with coverage, e2e Chromium, trace, bundle size, config check, OpenAPI drift, config-docs drift, neutrality check; `.github/workflows/nightly.yml` skeleton with conformance, load and mutation jobs (skipped until Phase 7) | TIO-TEST-001, TIO-TEST-008, TIO-DEPLOY-011 | ✅ DONE | 75ff0c2 | No Cloudflare token in GitHub, ever. Nightly cannot run `deploy.ts` (no Cloudflare token in GitHub, TIO-DEPLOY-006): the real runs happen in Workers Builds on every push to `main`; nightly reaches staging over HTTPS only |
| P0-04 | `scripts/trace.ts` + `scripts/trace.config.json`: parse spec ids and verification tags, parse test titles, fail on uncovered default-tag ids not allow-listed for the current phase, fail on unknown or withdrawn ids in tests, emit `doc/TRACEABILITY.md`; drift check for the generated file | TIO-TEST-006, TIO-TEST-007 | ✅ DONE | 6601661 | Allow-list shrinks as phases complete. Mapping is section → phase (`phases`) with per-id `overrides`; `describe` titles do not count, only `it`/`test` titles (incl. `.each`/`.for`) |
| P0-05 | `scripts/lint-rules.ts` (every rule in TIO-TEST-060), `scripts/check-ignores.ts` (≤ 15 justified istanbul ignores), `scripts/config-check.ts` (bindings equal §2.2, runtime deps equal the list, `nodejs_compat` absent, button profile has default ids), `scripts/neutrality-check.ts` (no 32-hex ids, no non-placeholder `database_id`), `scripts/bundle-check.ts` (≤ 1.5 MB) | TIO-TEST-060, TIO-TEST-003, TIO-ARCH-003, TIO-GEN-002, TIO-GEN-003, TIO-CFG-001, TIO-DEPLOY-005, TIO-PERF-002 | ✅ DONE | 6601661 | Runtime-dependency check is a subset check (fails on additions, does not require listed deps to be installed) because `knip` fails on unused dependencies; deps are added when first used |
| P0-06 | `src/env.ts`: `Env` bindings type, injected `Clock`, zod schema for env vars, secrets and settings with descriptions, defaults and bounds; settings loader with 60 s isolate cache and 1 h stale-if-error; cross-field validation; `MASTER_KEYS` validation at first request | TIO-CFG-002, TIO-CFG-003, TIO-CFG-005, TIO-ARCH-011, TIO-ARCH-012, TIO-ARCH-013, TIO-CRYPTO-010 | ✅ DONE | 75ff0c2 | Only place `Date.now()` is allowed. `http` ISSUER accepted only on loopback hosts (no environment flag, TIO-GEN-004); registrable domains approximated without a PSL (`src/util/domain.ts`); `SettingsLoader` 60 s TTL, 1 h stale-if-error; schemas live in `src/config/schema.ts` (no Workers types) and are re-exported by `src/env.ts` |
| P0-07 | `src/crypto/`: `random.ts`, `hash.ts` (SHA-256, constant-time compare), `master-keys.ts` (HKDF-derived keys per version), `envelope.ts` (AES-GCM handle codec for all six types), `uuid.ts` (v7 monotonic); property tests with fast-check for round-trip and single-bit tamper rejection | TIO-ARCH-006..009, TIO-CRYPTO-001..004, TIO-CRYPTO-020, TIO-DATA-001, TIO-DATA-002 | ✅ DONE | 75ff0c2 | |. `secretsEqual` uses `crypto.subtle.timingSafeEqual` (workerd-only), so its test lives in `test/component`; envelope property tests run in Node with fast-check |
| P0-08 | D1: `migrations/0001_init.sql` exactly as §4.1; `src/db/` repositories with typed parameters, `db.batch()` helper, Sessions API `withSession("first-primary")` helper; lint forbids `prepare(` elsewhere | §4.1, TIO-DATA-015, TIO-DATA-016, TIO-DATA-017 | ✅ DONE | 993c69b | |. Repositories land with the phase that first needs them (settings in Phase 0); `Db` wrapper counts reads and writes for the log line |
| P0-09 | Durable Objects: `src/do/UserDO.ts` with SQLite schema (§4.2 as amended), lazy `migrate()`, `init()`/`destroy()` guards, on-write purge; `src/do/InteractionDO.ts` with JSON document, alarm `deleteAll`, transition validator stub | §4.2, §4.3, TIO-DATA-019..023 | ✅ DONE | 993c69b | Two classes only. DO methods return `{ ok, error }` results rather than throwing so codes survive RPC; every mutating method takes `now` from the caller's Clock; state machine is a pure table in `src/interaction/state-machine.ts` |
| P0-10 | Router skeleton (`src/router/`): Hono app, security headers, CORS matrix, body and query limits, request id, `Host` check with health exception, 404/405 handling, JSON error model, uniform-error helper, structured log line, optional metrics, `GET /api/v1/health` | TIO-HTTP-001..006, TIO-ERR-001, TIO-ERR-002, TIO-OBS-001..003 | ✅ DONE | 6422772 | Header matrix test per route. `createApp({ clock, sink })` so tests inject the clock and capture log lines; one route table (`src/router/routes.ts`) drives CORS, 405 `Allow`, body limits and the header matrix; `OpenAPIHono` from Phase 0 so the OpenAPI drift check exists early; Host compared through the request URL (workerd does not expose the header); `minify: true` added to `wrangler.jsonc` because unminified zod alone is 787 KB |
| P0-11 | `scripts/gen-config-docs.ts` generating `doc/CONFIG.md` and `.dev.vars.example` from the schema in `src/env.ts`, with CI drift check; `scripts/gen-secrets.ts` printing `MASTER_KEYS` and `ADMIN_BOOTSTRAP_TOKEN` values for the README steps | TIO-CFG-005, TIO-DEPLOY-008 | ✅ DONE | c88b17e | |. OpenAPI route definitions live in `src/api/definitions.ts` (no Workers types) so Node generators can import them; `gen:openapi` added; `pnpm gen:secrets --dev-vars` writes `.dev.vars` for local runs |
| P0-12 | `scripts/deploy.ts` (profile selection, D1 migrations, D1 id resolution by name, `--var` injection, versions upload + smoke + deploy for production) and `scripts/smoke.ts`; unit tests with a fake `wrangler`; README deploy steps finalized | TIO-DEPLOY-007, TIO-DEPLOY-008 | ✅ DONE | 00c3f7f | Real run happens in nightly from Phase 2. Production path parses `Worker Version ID:` and `Version Preview URL:` from `wrangler versions upload` (verified against wrangler 4.135), so the production Worker must keep preview URLs enabled; generated config is `wrangler.generated.jsonc` (gitignored) |
| P0-13 | `test/support/`: `clock.ts`, `factories.ts` skeleton, `fetch-allowlist.ts` (fetchMock with network disabled and an allow-list), `virtual-authenticator.ts` stub interface | TIO-TEST-032, TIO-ARCH-016 | ✅ DONE | 00c3f7f | Authenticator implemented in P2-06. `fetchMock` no longer exists in `@cloudflare/vitest-plugin` 1.x: `fetch-allowlist.ts` is built on `@msw/cloudflare` (spec TIO-ARCH-016 and TIO-TEST-031 reworded, §13.1 row added); the authenticator interface arrives with its implementation in P2-06 instead of as an unused stub; tests reach the Worker through `test/support/op.ts` (`cloudflare:workers` `exports.default.fetch`, since `SELF` is deprecated) |
| P0-14 | Phase 0 exit: all gates green on `main`; `wrangler dev` answers `/api/v1/health`; trace allow-list configured for Phases 1–7 | §14.2 | ✅ DONE | 73a803a | Gates green locally and on the first push; `wrangler dev` answers `/api/v1/health`; Playwright smoke (health, JSON 404) on Chromium; `trace.config.json` `phase` stays 0 until Phase 1 exits (it names the last completed phase) |

---

## Phase 1 — Keys and discovery (complete)

**Goal:** the OP can publish JWKS and discovery and can sign and verify its own tokens.

**Exit criteria:** §5.2, §5.3 and §10 identifiers covered; `GET /.well-known/openid-configuration`, `/.well-known/oauth-authorization-server`, `/.well-known/jwks.json`, `/.well-known/webauthn` served with correct caching.

| ID | Task | Spec | Status | Commit | Notes |
|---|---|---|---|---|---|
| P1-01 | Key store: `src/crypto/keystore.ts` generating ES256 keys, exporting the JWK once, encrypting `private_jwk_enc` under the keystore key, importing non-extractable at runtime; `kid` = RFC 7638 thumbprint; first-key bootstrap guarded by `INSERT ... WHERE NOT EXISTS` | TIO-KEYS-010, TIO-KEYS-011, TIO-KEYS-014 | ✅ DONE | add37f6 | |. `src/crypto/secretbox.ts` seals private JWKs (and, later, upstream secrets) under the keystore key with the master-key version in the blob |
| P1-02 | Key roles derived from `activates_at`/`retired_at`; rotation creates (with `immediate`), cron retires superseded keys after `retire_after_seconds`, deletes retired rows after 90 days; fail closed when no active key; three-key lifetime test with the injected clock | TIO-KEYS-012, TIO-KEYS-013, TIO-CFG-010 (key steps) | ✅ DONE | add37f6 | Admin endpoints for keys arrive in P3-07. `maintainSigningKeys()` is the cron body; `scheduled()` wires it in P3-10. Key cache TTL is 60 s (spec §2.8 said 5 min; TIO-ARCH-011 wins and §2.8 was aligned). Same-second ties between active keys resolve to the newest row, then the greater kid |
| P1-03 | JWKS endpoint publishing every unretired key, no `d` member under any state, `Cache-Control: public, max-age=300`, served through `caches.default` | TIO-KEYS-001, TIO-KEYS-002 | ✅ DONE | add37f6 | |. Served through `caches.default`; cache hits are copied so the header middleware can decorate them |
| P1-04 | `src/oidc/capabilities.ts` constant; discovery documents for both well-known paths; deep-equality test against the constant; route-table comparison test; `/.well-known/webauthn` from `webauthn_origins` with the 5-label validation | TIO-DISC-001..004, TIO-PK-001 | ✅ DONE | add37f6 | Validators import the same constant in Phase 2. The protocol endpoints (`/authorize`, `/par`, `/token`, `/userinfo`, `/revoke`, `/logout`, `/federation/callback`, `/interactions/{id}/complete`) are in the route table now with a 501 `not_implemented` handler until Phase 2, so the discovery-vs-route-table test holds |
| P1-05 | JWT service (`src/crypto/jwt.ts` over jose): sign with header `alg`/`typ`/`kid` only and a 512-byte header test; verify own tokens against unretired keys with `iss`, `exp` (0 s leeway), `typ`; ID token builder (claims table, null omission, `at_hash`); `at+jwt` builder with `aud` from `audiences`; logout token builder | TIO-KEYS-015, TIO-TOKEN-030..034, TIO-LOGOUT-010 | ✅ DONE | add37f6 | |. Header sizes pinned at 106/110/115 bytes for JWT, at+jwt and logout+jwt; `ignoreExpiry` re-verifies an expired hint with the clock set to its `iat` |
| P1-06 | Master-key rotation: re-encrypt keystore rows under the active version in cron chunks; handles under retired versions rejected; three-phase test | TIO-CRYPTO-011, TIO-ARCH-008 | ✅ DONE | add37f6 | `POST /maintenance/rekey` endpoint in P3-07. `rekeySigningKeys(db, keys, limit)`; rows sealed under a version no longer in the secret are reported as unrecoverable, never modified |
| P1-07 | Phase 1 exit: trace shows §5.2, §5.3, §10 covered | §14.2 | ✅ DONE | 353b088 | |. `trace.config.json` phase = 1; 59 identifiers covered |

---

## Phase 2 — Core flow with passkeys

**Goal:** a standard OIDC client signs a user in with a passkey through the reference login app, end to end, on local `wrangler dev`.

**Exit criteria:** §5.4–§5.9, §6.1–§6.3, §6.6, §7 identifiers covered; `oauth4webapi` interop green; concurrency suite green for code, refresh, challenge, invitation, PAR and complete; Playwright passkey sign-up and sign-in green on Chromium.

| ID | Task | Spec | Status | Commit | Notes |
|---|---|---|---|---|---|
| P2-01 | Clients: repository, validation rules (redirect URI rules incl. loopback exception, grant/auth-method combinations, `audiences`, TTL bounds), secret generation and hashing, isolate cache with stale-if-error, disable semantics | TIO-CLIENT-001..004, TIO-CLIENT-010, TIO-CLIENT-011, TIO-ARCH-011 | ✅ DONE | 9526bdb | Phase 2 factories create clients through the OP's service layer (`src/oidc/clients.ts` + repository), the same functions the Admin API calls in P3-04, never raw SQL; TIO-TEST-032 is read that way until the Admin API exists. Validation, repository, cache and `createClient` done with tests; the disable semantics at `/token`, `/par` and refresh land with those endpoints. Spec TIO-DATA-003 reworded: generated ids use `[a-z0-9]` because base64url violates the lowercase pattern. Disable semantics at `/par`, `/token` (every grant) and refresh done at the Phase 2 exit (P2-17): the refresh grant of a disabled client revokes the family it presents (`client_disabled`) and answers `invalid_grant`, so the family stays dead after re-enabling; the rest is `invalid_client` |
| P2-02 | Client authentication for `/token`, `/par`, `/revoke`: `none`, `client_secret_basic`, `client_secret_post`, `private_key_jwt` (jose against `jwks`/`jwks_uri`, 60-second assertion window with mandatory `iat`); strict method enforcement; failed-auth rate-limit keys | TIO-TOKEN-002, TIO-TOKEN-003, TIO-TOKEN-004 | ✅ DONE | 83ffbb1 | No replay cache by decision (Appendix B #28). `src/oidc/client-auth.ts` (`authenticateClient`, pure over a parsed form and the `Authorization` header), `src/oidc/jwks-cache.ts` (jose remote sets, LRU 100, unknown-`kid` refetches spaced by a 30 s cooldown) and `src/router/form.ts` (TIO-TOKEN-001 content type and duplicates). The failure result carries the client id for the TIO-TOKEN-004 limiter key; the limiter call and `WWW-Authenticate` are wired at the endpoints (P2-10, P2-04, P2-11) with the binding of P6-05 |
| P2-03 | `/authorize` validation pipeline in spec order, non-redirectable errors to `login_url`, redirectable errors with `iss`, `prompt`/`max_age`/`login_hint` handling, session evaluation, stale-parameter rule, `prompt=none` never touching the session, `allowed_groups` outcome | TIO-AUTHZ-001..024 (013 withdrawn) | ✅ DONE | 1a0ee7b | Depends on P2-08 for `authorizeWithSession`. `src/oidc/authorize.ts` (steps 5–12, shared with `/par`), `src/oidc/authorize-endpoint.ts` (steps 1–4 and 14), `src/oidc/handles.ts` (session, code and binding handles), `src/router/cookies.ts`, `src/oidc/interactions.ts`. A pushed request is taken with `InteractionDO.claimPushed` (exactly one taker, client-bound, fresh binding and TTL) before the session is evaluated, so two `/authorize` calls on one `request_uri` cannot both issue a code. The second TIO-AUTHZ-024 test (`prompt=login` interaction, then a bare `/authorize` on the new session is a hit) lands with `/complete` in P2-09. 19 HTTP tests |
| P2-04 | PAR endpoint: client auth, validation reuse, `request_uri` single use and 60 s expiry stored in `InteractionDO`, `require_par` enforcement | TIO-PAR-001..004 | ✅ DONE | 9f6f437 | `src/oidc/par-endpoint.ts` and `src/oidc/token-common.ts` (form + client authentication shared with `/token` and `/revoke`). `src/router/rate-limit.ts` started here for TIO-PAR-004/TIO-TOKEN-004: the §6.7 key table over the two bindings. Spec conflict for P6-05: §6.7 lists different limits per key class (20/60 s failed auth, 60/60 s navigation, 600/60 s admin) but §12.1 declares only two `simple` bindings, which carry one limit each; until the bindings are split, every class on a binding shares its limit |
| P2-05 | Interaction API: origin and binding-cookie checks, `GET` document (masking, field allow-list test), state machine table test, attempt counter, `abort`, consent endpoint and grant storage, logout interaction confirm | TIO-IX-001..003, TIO-IX-010, TIO-IX-011, TIO-IX-020, TIO-IX-021, TIO-IX-040, TIO-IX-041, TIO-IX-050, TIO-IX-070, TIO-CONSENT-001..004, TIO-RL-002 | ✅ DONE | b7a0ff9 | Federation endpoint arrives in P4-02. `src/interaction/api.ts`: guard (id shape, origin or same-site GET, binding cookie, pushed and terminal rules), GET document, `consent`, `abort`; `InteractionDO.patch` and `attempt` (the call past the limit fails the interaction, TIO-RL-002); `interactions` CORS class in `headers.ts`; OpenAPI definitions for the JSON API routes (`registerPath`, plain handlers, JSON bodies through `readJsonBody`). Error statuses: 403 origin/binding/too_many_attempts, 404 not found, 409 invalid state, 401 passkey codes. TIO-IX-011 and TIO-IX-070 covered by the `passkey/verify` tests of P2-06; `logout` confirm is Phase 5 (TIO-IX-050, P5-01). TIO-ARCH-010 (API surface enumeration) and TIO-IX-010 citations added at the Phase 2 exit |
| P2-06 | Passkeys: registration and authentication options, verification via `@simplewebauthn/server` inside `UserDO.verifyAssertion` (atomic counter), counter policy, `credProps.rk` rule, `userHandle` routing with `passkey_index` fallback, limits and naming; `test/support/virtual-authenticator.ts` with fault injection, itself verified with the library | TIO-PK-002, TIO-PK-010..014, TIO-PK-020..023, TIO-PK-030, TIO-PK-040, TIO-PK-041, TIO-IX-030, TIO-TEST-030 | ✅ DONE | b37af2f | Options, registration and assertion verification (`src/auth/passkey.ts`), `UserDO.verifyAssertion` and the virtual authenticator (ES256, EdDSA, RS256, packed self-attestation, fault injection) done with 12 unit and 3 component tests; `@simplewebauthn/server` runs in workerd without `nodejs_compat`. Attestation statements of other formats are stripped to `none` before verification (TIO-PK-012, `attestation_policy = ignore`). Only §7.8 error codes are used: an unsupported algorithm or a bad credential id length is `passkey_verification_failed` with a log-only `reason`. `userHandle` routing with the `passkey_index` fallback (orphan rows deleted on discovery, TIO-DATA-026), `passkey/options` and `passkey/verify` on the Interaction API (`src/interaction/passkey.ts`), `src/users/create.ts` (§4.6 creation order), `src/users/passkeys.ts` (DO then index, rollback), `src/db/users.ts`, `src/db/groups.ts` and `test/support/{http,passkeys,sessions}.ts` done. TIO-PK-040 (last passkey) is Phase 5 (self-service); TIO-PK-041 (names) done in P2-07 |
| P2-07 | Registration policy and invitations: `register`/`recover` invitations as `tio_iv` handles, atomic consumption, pre-filled fields, `email_in_use` hint, recovery always revoking sessions and families; Interaction API register endpoints | TIO-REG-001..005, TIO-IX-032, TIO-REC-001 | ✅ DONE | cafb69c | `src/db/invitations.ts`, `src/users/invitations.ts` (`createInvitation` mints the `tio_iv` handle; `openInvitation` tells invalid, used and expired apart), `src/interaction/register.ts`. The interaction document gained a `registration` section (the draft account: email, verified flag, display name, groups) alongside `passkey_challenge.pending_uid`/`invitation_id`, since the verify body carries only the WebAuthn response and a name. A credential already indexed is refused before the invitation is consumed; an invitation consumed for a ceremony that then fails to create the account stays consumed (TIO-IX-032, by design). Admin creation of invitations is P3-04; the bootstrap invitation is P2-13 |
| P2-08 | `UserDO` core: sessions (create, rotate, touch, expire, revoke with family cascade), `authorizeWithSession`, `issueCode`, `exchangeCode` (atomic, PKCE, replay revokes families), refresh families and `rotateRefreshToken` (reuse detection, serial check, idle/absolute, session binding), grants with `client_created_at`, purge | TIO-SESS-001..006, TIO-TOKEN-010..014, TIO-RT-001..010, TIO-CLIENT-005, TIO-AUTHZ-022, TIO-AUTHZ-023 | ✅ DONE | deefbb9 | Largest task; split into sub-commits by table. Core done with 25 component and 2 concurrency tests. `refresh_families.code_secret_hash` added to §4.2 so TIO-TOKEN-012 can revoke the families a replayed code created; `setDisabled()` added early because the disabled-user checks needed it; session ids are generated by the caller (UUID v7 from the Clock). Completed by the endpoints of P2-09..P2-11 and the concurrency suite of P2-16 |
| P2-09 | `/interactions/{id}/complete`: binding check, session creation or rotation, code issuance, cookie handling, error redirect, resume and already-completed cases, revoked-session case | TIO-IX-033, TIO-IX-060..062 | ✅ DONE | 0447b9a | `src/interaction/complete.ts`; `src/obs/request-meta.ts` (ip hash under `tio/v1/iphash`, `Family/major` user agent, `cf.country`). Consent-only interactions issue the code on the session whose cookie the browser still presents (re-verified at `/complete`, as TIO-IX-050 does for logout); a missing or revoked session restarts the interaction as `login_required` (TIO-IX-062). Same-user re-authentication rotates the session; a rotation target that vanished is replaced. The code binds the consented scopes. Second TIO-AUTHZ-024 test lives here |
| P2-10 | `/token` endpoint: form parsing and limits, grants `authorization_code`, `refresh_token`, `client_credentials`, response shape, storage error mapping, scope rules including `admin` membership check | TIO-TOKEN-001, TIO-TOKEN-005, TIO-TOKEN-020, TIO-TOKEN-021, TIO-SCOPE-001, TIO-SCOPE-002, TIO-ARCH-014, TIO-ARCH-015 | ✅ DONE | 7d9779f | `src/oidc/token-endpoint.ts`. The family kind is decided inside `UserDO.exchangeCode` from the code's scope and the client's `offline_access` flag (TIO-TOKEN-014), so the endpoint passes `offline_allowed`; `/authorize` refuses `offline_access` for clients without the flag. `UserContext.sid` is nullable: offline families carry no `sid` in either token. TIO-SCOPE-002 enforced at passkey verification, session hit, code issuance, exchange and refresh (`UserDO.adminAllowed`); `UserDO.setGroups` added. 9 HTTP tests through the full login flow |
| P2-11 | `/userinfo` (GET and POST, fresh profile from `UserDO`, scope-gated claims) and `/revoke` (family revocation, foreign-token no-op, access-token session-family case) | TIO-UINFO-001..003, TIO-REV-001..003 | ✅ DONE | b2bd4f8 | `src/oidc/bearer.ts` (bearer extraction and `verifyAccessToken`, reused by the Self-service and Admin APIs with `acceptClient = false`), `userinfo-endpoint.ts`, `revoke-endpoint.ts`. A token whose `aud` is only the client's `audiences` is refused at `/userinfo` (TIO-TOKEN-033/034); the `account` scope adds the issuer. Client tokens (no `auth_time`) are 401. `token.revoke` / `token.revoke_foreign` are log lines until the audit pipeline (P6) |
| P2-12 | Groups: `groups` claim from `UserDO`, `allowed_groups` enforcement at authorization and refresh, `admins` system group bootstrap | TIO-DATA-011..013, TIO-AUTHZ-017, TIO-SCOPE-002 | ✅ DONE | f2ff0e9 | Group CRUD endpoints in P3-03. `src/db/groups.ts` (system groups refuse rename and delete), `src/users/groups.ts` (`ensureAdminsGroup`, `setUserGroups`: DO first, then the mirror; `partial_failure` on a failed mirror write), `UserDO.setGroups`. `allowed_groups` and admin-membership enforcement landed with P2-03, P2-06 and P2-10 |
| P2-13 | Admin bootstrap endpoint (single use, constant-time token compare, `admin-cli` client, first admin invitation) so tests and dev can create the first client and user without backdoors | TIO-ADMIN-010, TIO-ADMIN-011, TIO-GEN-004 | ✅ DONE | f2ff0e9 | `src/admin/bootstrap.ts`; the response carries the raw invitation token besides `invitation_url` (null without `login_url`) so a deployment can be bootstrapped before the login app is configured; an `admin-cli` client left by an interrupted attempt is reused. The bootstrap→register→admin-scope flow is tested end to end. Full Admin API is Phase 3 |
| P2-14 | Reference login app `examples/login-app/` (static, dependency-free, every interaction state) served through the `ASSETS` binding under `/login/` when `BUNDLED_LOGIN_APP=true`, with default `login_url`/`login_origins`; `not_configured` behavior | TIO-IX-080, TIO-IX-081, TIO-CFG-004, TIO-GEN-001 | ✅ DONE | 9147917 | `examples/login-app/{index.html,app.js,style.css}` (every state: sign-in, sign-up with invitation, consent, link, logout, ready/failed/completed, non-redirectable errors; WebAuthn JSON fallbacks for browsers without the `*FromJSON` statics; a bootstrap invitation link parks the token in `sessionStorage` until an interaction arrives). `src/router/login-app.ts` serves the binding under `/login/*` (path rewritten to the assets root) with a same-origin CSP and no WebAuthn denial; `doc/LOGIN_APP_GUIDE.md` written. Browser-driven coverage of the app comes with the P2-15 e2e suite |
| P2-15 | Interop and e2e: `oauth4webapi` code flow, refresh, PAR, `private_key_jwt`, revocation, userinfo inside workerd; `examples/rp-node` with `openid-client` v6; Playwright passkey sign-up and sign-in on Chromium, Firefox, WebKit via `browserContext.credentials`; RP never touches OP storage | TIO-TEST-033, §13.5 interop and e2e rows | ✅ DONE | 861de99 | CDP virtual authenticator for negative UV cases. `test/interop/oauth4webapi.test.ts` (discovery, PKCE code flow with JWKS signature validation, userinfo, refresh, revocation, PAR with `private_key_jwt`, `client_credentials` with `client_secret_basic`) runs on the real clock because the library stamps assertions itself. `examples/rp-node/server.ts` (openid-client v6, JSON responses) is Playwright's second web server; `scripts/e2e-server.ts` starts `wrangler dev` on `http://localhost:8787` with a fresh `--persist-to` directory, migrations and `registration.mode = open` written by `wrangler d1 execute` (an operator action, no test-only OP code), and overrides `ISSUER`/`RP_ID` with `--var` because browsers do not accept an IP literal as RP ID. Playwright has no `browserContext.credentials` API: Chromium uses the real WebAuthn stack through the CDP virtual authenticator (credentials exported and re-added across contexts); Firefox and WebKit get a page-side shim (`test/e2e/support/webauthn-shim.ts`) that hands ceremonies to the software authenticator over `exposeFunction`, so the OP still verifies real signatures. Bootstrap runs once per run in a Playwright global setup; 7 e2e tests green on Chromium and Firefox locally (Firefox unpacked by hand like Chromium); WebKit not installed on this workstation |
| P2-16 | Concurrency suite for this phase: parallel code exchange, refresh rotation, passkey challenge, invitation, PAR `request_uri`, `/complete` | TIO-TEST-010 (rows 1–5, 7), TIO-RT-003 | ✅ DONE | effe958 | `test/concurrency/http.test.ts`. Spec conflict: TIO-TEST-010 row 3 races 20 passkey verifications while §7.4 caps attempts at 10, so the losers see 401/403/409 and the interaction may end `failed`; the test asserts one winner and one counter move. Finding: `InteractionDO` read-check-writes (`claimPushed`, `claimCompletion`, `apply`, `attempt`) were not exactly-once without input gates (the vitest runtime); every write now runs under `blockConcurrencyWhile`. `/complete` gained a `completing` claim (TIO-IX-061). |
| P2-17 | Phase 2 exit: trace shows §5.4–§5.9, §6.1–§6.3, §6.6, §7 covered; staging deploy exercised for real by nightly | §14.2 | ✅ DONE | 68e6e86 | `scripts/trace.config.json` phase = 2 (0 errors). Exit criteria met locally and in CI: trace, oauth4webapi interop, concurrency suite, Playwright on Chromium. Seven Phase 2 identifiers had no citing test and got one here: TIO-ARCH-002 (lint rule `do-addressed-by-entity-id` + real-tree test; `userStub` is now the only user addressing), TIO-ARCH-009 (`test/http/forged-handles.test.ts`), TIO-ARCH-010 (surface enumeration + claim probe), TIO-CLIENT-002, TIO-PK-011, TIO-IX-010 (citations), TIO-TEST-032 (lint rule `factories-through-public-apis`). TIO-CLIENT-004 gained its HTTP test and the lazy family revocation. Remaining: the staging nightly needs OP-01 (owner) Closed 2026-09-22: the last exit criterion, a staging deploy made for real by the push pipeline, holds since the owner fixed the `TIO_ISSUER` build variable — every push to `main` deploys staging through Workers Builds and `deploy.ts` (health reports the pushed commit). |

---

## Phase 3 — Admin API

**Goal:** everything an operator needs is an authenticated JSON call, documented by a generated OpenAPI 3.1 file.

**Exit criteria:** §9 identifiers covered; `doc/openapi.json` committed and drift-checked; import benchmark measured on staging.

| ID | Task | Spec | Status | Commit | Notes |
|---|---|---|---|---|---|
| P3-01 | Admin authorization model (scope `admin`, `aud ∋ ISSUER`, live `admins` membership, service clients), audit of every mutation, secret-free responses, keyset pagination with signed cursors, conflict codes | TIO-ADMIN-001..004 | ✅ DONE | 3b8aac4 | `src/admin/auth.ts` `requireAdmin` (401 `invalid_token` / 403 `insufficient_scope` with `WWW-Authenticate`, 429 per token jti, 503 on storage failure; a user's membership read from `UserDO.getProfile` every request, a service client's record from the cache); `src/audit/events.ts` `Auditor` (per-request §11.1 events, flushed to the log at request end; queue producer is P6-02) and `src/audit/diff.ts` `boundedDiff` (secret fields as `{changed: true}`); `src/admin/pagination.ts` (HMAC cursors under `tio/v1/cursor`, listing-bound, 1 h); `GET /admin/users` built here as the vehicle (`src/db/users.ts` `listUsersStatement`, row-value keyset on `users_created`; 10,000-row test asserts the plan and `rows_read ≤ limit + 1`). Conflict codes land with their endpoints. `test/support/admin.ts` obtains admin tokens through real logins |
| P3-02 | Users endpoints: list/filter, create (D1 claim → DO init → activate), get, patch, disable/enable, delete, passkeys, identities, sessions, refresh families, grants, events (from `audit_hot`, view lands in P6-03), invitations, reindex, export, restore from DO bookmark | §9.4 Users, TIO-DATA-009, TIO-DATA-010, TIO-DATA-026, TIO-DATA-027, TIO-DEPLOY-003, TIO-PRIV-002 | ✅ DONE | 4204c13 | `GET /users` (filters, paging) done in P3-01; `events` answers 501 until P6-03. 24 routes in `src/admin/users.ts` over `src/users/admin.ts` (profile update, disable/enable, delete, reindex) and new `UserDO` methods (`updateProfile`, `listFamilies`, `revokeFamiliesOfClient`, `counts`, `exportState`, `restore`, `grantClientIds`); `createUser` links identities at creation (`identity_index` claims). `partial_failure` is a boolean on the 200 body and the audit event's `reason`. Restore: the local Durable Object backend has no point-in-time recovery, so the success path (bookmark applied, object aborted in `waitUntil`) carries the first two istanbul ignores (2/15); the 503 `restore_unavailable` path is tested. Back-channel logout on disable/delete is Phase 5. Recovery invitations done here (P3-06 keeps the register ones) |
| P3-03 | Groups endpoints and membership with dual-write reporting (`partial_failure`) | §9.4 Groups, TIO-DATA-011..013 | ✅ DONE | 135ecc5 | `src/admin/groups.ts`, `src/users/groups.ts` (`addMembership`, `removeMembership`, `propagateToMembers`), migration `0002_groups_created.sql` (keyset index). Decisions: a rename or deletion first rewrites the group list in every member's object (concurrency 20, at most 1,000 members per call, else 409 `group_too_large`; `propagation.failed` lists members to retry) because the objects are authoritative for the `groups` claim; a rename pre-checks the new name and reverts the propagation on a late collision. Spec gap: §11.2 has no `group.*` events, so `group.created`, `group.updated` and `group.deleted` are emitted and must join the P6-01 catalog. The admin rate limit is keyed by the whole `jti` (a UUID v7 prefix is its timestamp, shared by tokens minted in the same millisecond) — §6.7 says "jti prefix". Fault injection for the admin suites is `test/http/faults.ts` (`brokenD1`, `failingD1`, `brokenDoFor`, `sabotageDo`) |
| P3-04 | Clients endpoints: CRUD, rotate-secret (immediate), disable/enable, deletion with lazy cleanup test (delete, re-create same id) | §9.4 Clients, TIO-CLIENT-003..005 | ✅ DONE | 3a693ff | `src/admin/clients.ts`; `updateClientRecord` in `src/oidc/clients.ts` lays the patch over the stored record and validates the whole (schema defaults never replace stored values); a switch to a secret method mints a secret returned once, a switch away drops the hash; `rotate-secret` on a secretless client is 409 `no_secret`. Listing walks `clients_created` (migration `0003_clients_created.sql`); a row that no longer decodes still counts for paging but is not shown. `publicClient` moved to `src/oidc/clients.ts`. The lazy-cleanup test (`test/http/admin-clients.test.ts`, TIO-CLIENT-005) deletes and re-creates a client under the same id and asserts the old family is `invalid_grant` and a session hit asks for consent again |
| P3-05 | Upstreams endpoints: CRUD with discovery fetch and validation on create/update, secrets encrypted, `test` endpoint | §9.4 Upstreams, TIO-FED-001, TIO-FED-002 | ✅ DONE | 88c8fcc | Login flow is Phase 4. `src/federation/upstreams.ts` (model, validation, `publicUpstream` with `redirect_uri`), `src/federation/discovery.ts` (`fetchDiscovery`, `fetchJwks`, 5 s timeout, injectable fetch), `src/db/upstreams.ts` (migration `0004_upstreams_created.sql`), `src/admin/upstreams.ts`. `client_secret` and `client_jwk` are write-only and sealed under the keystore key; `has_client_secret`/`has_client_jwk` say what is stored. Auto discovery is fetched on every create and update (a failure refuses the change and emits `upstream.discovery_failed`); `test` refetches discovery (or takes the manual endpoints) and the JWKS and reports both steps. The isolate metadata cache of §2.8 arrives with the login flow (P4-01) |
| P3-06 | Invitations endpoints (create with one-time token and URL, list, get, revoke) | §9.4 Invitations, TIO-REG-002 | ✅ DONE | 2c0155b | `src/admin/invitations.ts` (register invitations; recovery ones are created under `/users/{id}/invitations`, P3-02, and share `publicInvitation`/`invitationUrl`). Listing walks `invitations_created` (migration `0005_invitations_created.sql`) with `kind` and `user_id` filters. Revocation deletes the row, so the token opens as `invitation_invalid` |
| P3-07 | Keys endpoints (`GET /keys` with derived role, `POST /keys/rotate` with `immediate`, `DELETE /keys/{kid}` with `last_active_key` refusal), settings endpoints, `/stats`, maintenance (`reindex`, `purge`, `rekey`) | §9.4 Keys/Settings/Maintenance, TIO-KEYS-012, TIO-KEYS-013, TIO-CFG-003, TIO-CRYPTO-011 | ✅ DONE | 6a00e56 | `src/admin/system.ts`. The cron body lives in `src/maintenance/run.ts` (`runMaintenance`: audit purge ≤ 10 × 1,000 rows, expired invitations beyond 30 d, `creating` repair after 60 s / drop after 1 h, `deleting` completion, key rotation and retirement, a rekey chunk of 50 signing keys and 50 upstreams; wall-time budget checked between steps and per row; `system.cron_run` event; `system.last_cron_run` in the settings table for `/stats`) so P3-10 only wires `scheduled()`. `GET /settings` is `name → {value, source}`; `PATCH` takes `null` for a default, refuses `bootstrapped_at`, validates the merged whole (`invalid_settings`), invalidates the isolate cache. `DELETE /keys/{kid}` retires; a token signed by a retired key is refused at once (tested). `rolesByKid` added to the keystore. `maintenance/reindex` walks the registry keyset 100 per call with a `reindex` cursor |
| P3-08 | Bulk import `POST /import/users` (NDJSON, idempotent per line, 50 concurrent creations) and `perf/seed.ts`; measure on staging | TIO-ADMIN-020, TIO-ADMIN-021 | ✅ DONE | f5985ea | Target time verified in P7-04; the staging measurement waits on OP-01. `src/admin/import.ts` (a pool of 50 lines in flight, results in input order, `application/x-ndjson` out); `create_invitation` mints a `recover` invitation for the created user (the imported account already exists, so a `register` one would be wrong). Lines run concurrently, so duplicates within one batch race and the loser is `conflict`; idempotency holds across batches. `scripts/perf/seed.ts` writes deterministic NDJSON (`--users`, `--groups`, `--identities`, `--seed`) |
| P3-09 | OpenAPI 3.1 generation with `@hono/zod-openapi` (`doc31`), served at `/api/v1/openapi.json`, committed snapshot `doc/openapi.json`, drift check | §5.1, §14.1 | ✅ DONE | 991ee04 | Built up task by task since P2-05: `src/api/definitions.ts` (`registerApi` with the `adminToken` bearer scheme), `pnpm gen:openapi`, the `git diff --exit-code` drift check in CI and `test/http/openapi.test.ts` (served = committed; every Admin API operation but bootstrap carries `security` and the 401/403/429 responses; every documented operation is in the route table) |
| P3-10 | `scheduled()` handler: audit purge placeholder, invitation cleanup, `creating` repair, `deleting` completion, key rotation and retirement, rekey chunk, retired-key deletion, `system.cron_run`; tests with `createScheduledController` | TIO-CFG-010, §4.6 | ✅ DONE | ce42c7f | `src/maintenance/scheduled.ts` `createScheduled` wraps `runMaintenance` (P3-07) with a system-actor auditor and its own settings loader; the default export of `src/index.ts` carries it. The audit purge is real, not a placeholder: `audit_hot` rows beyond `audit.hot_retention_days` go in batches of 1,000 (P6-04 keeps only the verification of the windows). Invalid configuration and storage failures are logged, never thrown |
| P3-11 | Phase 3 exit: trace shows §9 covered | §14.2 | ✅ DONE | 991ee04 | `scripts/trace.config.json` phase = 3 (0 errors). TIO-ARCH-013 closed by reading D1 directly in `GET /admin/settings` and the grants listing (tested against the caches); TIO-PRIV-001 by `test/scripts/privacy.test.ts`, which pins the columns of every user-related table. The import benchmark on staging (P3-08) waits on OP-01 like the Phase 2 nightly |

---

## Phase 4 — Federation

**Goal:** users sign in through Google, Microsoft or any compliant upstream, with the account-resolution policy enforced.

**Exit criteria:** §6.4 identifiers covered; security-suite federation cases green; outbound allow-list test green; Google and Microsoft verified manually and recorded.

| ID | Task | Spec | Status | Commit | Notes |
|---|---|---|---|---|---|
| P4-01 | Upstream metadata and JWKS caching (1 h, stale-if-error 24 h, single refetch on unknown `kid` per 5 min) | TIO-FED-001, §2.8 | ✅ DONE | c8269b4 | `src/federation/metadata.ts` (`UpstreamMetadataCache`, per isolate, keyed by alias; manual discovery never fetches; the Admin API's PATCH/DELETE invalidate the alias) and a second `RemoteJwksCache` (`upstreamJwks`, 5-minute cooldown) in `src/router/app.ts`. The stale-for-a-day rule is tested across the leg (started at 23 h 57 min, callback after the mark fails, the next start is 503 `upstream_unavailable`) |
| P4-02 | Outbound authorization request builder with PKCE, nonce, `tio_fs` state, `extra_authorize_params`, `forward_login_hint`; Interaction API `POST …/upstream/{alias}`; federation leg expiry | TIO-FED-010, TIO-FED-011, TIO-IX-040 | ✅ DONE | c8269b4 | `src/federation/outbound.ts` (`upstreamHandler`): `login_required` only (409), unknown and disabled aliases the same 404, an optional `register` invitation opened at the start and remembered in the leg, 503 `upstream_unavailable` when discovery fails. The leg (`InteractionDO.federation`: alias, `state_hash`, nonce, `code_verifier`, `expires_at` = now + 300, `invitation_id`) replaces any previous one. `sealFederationHandle`/`openFederationHandle` in `src/oidc/handles.ts`; `forward_login_hint` is a new upstream column (migration `0006_upstreams_login_hint.sql`) |
| P4-03 | `/federation/callback` (GET and POST): state decryption and single-use consumption, binding cookie, upstream `error` handling, code exchange with `client_secret_basic`/`client_secret_post`/`private_key_jwt`, timeouts | TIO-FED-020..022 | ✅ DONE | c8269b4 | `src/federation/callback.ts` and `src/federation/token.ts` (`exchangeCode` with the three client authentication methods and a 60 s `private_key_jwt` assertion, `fetchUserinfo`, 10 s / 5 s timeouts, injectable fetch). `InteractionDO.consumeFederation` takes the leg exactly once (state hash, unexpired, `login_required`). Failures before the leg is found go to the login app with `?error=` (`invalid_state`, `interaction_binding_failed`); after it, the interaction fails with §7.8 codes and an `identity.login_failed` event whose `reason` names the step (`upstream:<code>`, `token_status_401`, `id_token:nonce`, `storage`, …). Only OAuth error codes travel on from the upstream; anything else becomes `upstream_error` |
| P4-04 | ID-token validation (jose, every condition a negative test), optional userinfo with `sub` equality, `required_claims`, `claims_map`, `trust_email_verified` boolean rule | TIO-FED-030..033 | ✅ DONE | c8269b4 | `src/federation/id-token.ts` (`verifyUpstreamIdToken` with one reason per rule: malformed, alg, signature, iss, aud, azp, exp, iat, nonce, sub; `extractClaims`; `missingRequiredClaim`). Every negative case runs through the fake upstream's faults in `test/http/federation.test.ts`; the cases jose reports differently (non-base64url signature, unreadable key set, future `nbf`) in `test/component/federation-units.test.ts`. Key rotation refetches the JWKS once |
| P4-05 | Account resolution: identity lookup, verified-email `link_required` path with passkey re-authentication by the candidate, auto-create and registration policy, `amr`/`acr`, disabled user; finalization from the callback when ready | TIO-FED-040..043, TIO-IX-031 | ✅ DONE | c8269b4 | `resolveAccount` in `src/federation/callback.ts` in the order of TIO-FED-040 (index → object confirmation with `UserDO.touchIdentity`, a stale index row released per TIO-DATA-026; verified email of an existing user → `link_required` or `account_exists` under `federation.link_by_verified_email: never`; creation under `federation.auto_create` or a register invitation, refused by `registration.mode: closed`; an invitation names the email and its verification). The shared authentication step (`authenticateInteraction` in `src/interaction/passkey.ts`) handles `amr`/`acr`/`upstream`, the disabled user (`access_denied`, nothing about the upstream), group and admin-scope checks and consent; `passkey/verify` in `link_required` refuses another user with 403 `link_wrong_user` (counted as an attempt) and links the candidate (`linkIdentity`: D1 claim, then the object, released on failure). The document's `link` section masks the email. `createUser` errors are reported as they are (`email_invalid`, `group_unknown`, …) |
| P4-06 | Unlinking rule in Self-service and Admin (`last_login_method`) | TIO-FED-051, TIO-PK-040 | ✅ DONE | c8269b4 | Spec conflict: TIO-FED-051 states the rule without an actor while §9.4 says "Admin unlink has no last-method rule" and TIO-PK-040 exempts the Admin API for passkeys. Decision: the rule binds the user, not the administrator. `UserDO.removePasskey`/`removeIdentity` take a `RemovalActor` (`"admin"` by default, `"self"` refuses with `last_login_method` when the row is the user's only passkey or identity), tested in `test/component/user-do.test.ts`; the `/me` endpoints of P5-03 pass `"self"` |
| P4-07 | Fake upstream: `test/support/fake-upstream/` pure module with fault injection, mounted via the outbound interceptor (`@msw/cloudflare`); standalone Worker entry for staging with deploy refusal outside staging; outbound allow-list test across all flows | TIO-TEST-031, TIO-ARCH-016 | ✅ DONE | c8269b4 | `test/support/fake-upstream/index.ts` (`FakeUpstream`: discovery, authorize with `x_sub`, token with the three client authentications, JWKS with rotation, userinfo; 26 faults by `fault=` query or `x-fake-upstream-fault` header, unknown names throw; `discoveryDown`, `withoutUserinfo`). `worker.ts` + `wrangler.jsonc` are the standalone Worker (one Durable Object holds the provider; configured by `TIO_FAKE_*` from the deploy environment); `pnpm run deploy:fake-upstream` (`scripts/deploy-fake-upstream.ts`, `deployFakeUpstream`) refuses every profile but staging. `test/http/outbound.test.ts` runs the passkey, federation, `private_key_jwt`-by-`jwks_uri`, Admin upstream-test and refresh flows under the interceptor (`test/support/fetch-allowlist.ts` records every outbound request) and asserts the exact endpoint set; an unmounted origin is refused. The staging deployment itself is OP-06 |
| P4-08 | Google and Microsoft (single tenant) verification on staging, recorded in `doc/adr/0017-upstream-verification.md` | §6.4 | 🔧 IN PROGRESS | — | Google verified on 2026-09-22 (ADR 0017): registered on staging with the owner's client, discovery and JWKS fetched by the OP, a real login by the owner through the example relying party — ID token with `amr ["fed"]`, verified email and name, the account, identity, session and audit trail on staging as §6.4 and §11.2 say. A second pass the same day added single sign-on over a real session (a second authorization served with no interaction, seven hours and fifty-five minutes after the first) and a second Google account auto-creating its own staging account; a third pass with `prompt=login` closed TIO-FED-040 step 1 — the index resolved the same account (no `user.created`, no `identity.linked`), the identity's `last_login_at` was written and the session was rotated in place with a new `auth_time` and the same `sid` (TIO-SESS-002). Google is verified over every branch of §6.4.5 a real provider can reach. Microsoft waits for its Entra registration (OP-04), tracked as [issue #2](https://github.com/fdcastel/tiny-oidc/issues/2). Phase 5 did not depend on it |
| P4-09 | Phase 4 exit: trace shows §6.4 covered | §14.2 | 🔧 IN PROGRESS | c8269b4 | `scripts/trace.config.json` at phase 4: 221 covered, 0 errors; 421 tests, 100% coverage; outbound allow-list test green. Open exit criterion: the manual Google/Microsoft verification (P4-08). Phase 5 starts early: its tasks are independent of that criterion |

---

## Phase 5 — Logout and self-service

**Goal:** sessions end everywhere they should, and first-party apps can build security settings screens.

**Exit criteria:** §5.10 and §8 identifiers covered; e2e logout cases green.

| ID | Task | Spec | Status | Commit | Notes |
|---|---|---|---|---|---|
| P5-01 | RP-initiated logout: hint verification (expiry ignored, retired key fails), `post_logout_redirect_uri` matching, hinted-session revocation, no-hint confirmation interaction, landing URL | TIO-LOGOUT-001..005, TIO-IX-050 | ✅ DONE | 1c7af2a | `src/logout/rp-logout.ts` (`logoutHandler`, `endSession`, `notifyEndedSessions`, `postLogoutDestination`), `src/interaction/logout.ts` (`POST …/logout {confirm}`, `logout_decision` → `ready` with the decision kept on `LogoutRequest.decision`), the logout branch of `/complete` (ends the session only on a confirmation and only while the browser presents the cookie the interaction was made from). Decisions: a hint whose `client_id` disagrees with its audience, a multi-audience hint and an unknown audience are `invalid_request`; a hint without `sid` (offline ID token) ends nothing; `post_logout_redirect_uri` matching reuses the `/authorize` matcher, so loopback clients (RFC 8252) may come back on any port (TIO-CLIENT-011's single exception; TIO-LOGOUT-002 says "exactly"); the bootstrap `admin-cli` client registers `http://127.0.0.1:0/loggedout`. `state` is echoed only to the client's URI, never to the landing URL. `session.revoked`, `logout.rp_initiated` and `logout.confirmed` (declines are `outcome: failure`, `reason: declined`) are emitted; `interaction.*` events wait for P6-01 |
| P5-02 | Back-channel logout: token delivery in `waitUntil`, queue-based retries with the delay schedule, `logout.backchannel_failed`, triggers from every revocation path (self-service, admin, disable, delete) | TIO-LOGOUT-011..013, TIO-DATA-009 | ✅ DONE | 1c7af2a | `src/logout/backchannel.ts` (`notifyClients`: one `logout+jwt` per client with a `backchannel_logout_uri`, delivered in `waitUntil` with a 5 s timeout, `logout.backchannel_sent`; a failure queues `{kind: backchannel_logout, attempt: 1}` with a 30 s delay; `retryBackchannel`: re-mints under the same `jti`, five retries at 30 s, 2 min, 10 min, 30 min, 2 h, then `logout.backchannel_failed`). `src/queue/consumer.ts` (`createQueue`, exported as `queue()` from `src/index.ts`) dispatches on `kind`; unknown kinds and unreadable tasks are acknowledged and logged, a missing configuration retries the batch, unavailable keys retry the message; the audit batches of TIO-AUDIT-011 join it in P6-02. Triggers wired: `/logout`, the logout interaction, `/complete` replacing another user's session, Admin session deletion (one and all), disable, delete, recovery invitations, and the Self-service sessions endpoints. Queue sends that fail are logged, never thrown (TIO-AUDIT-012) |
| P5-03 | Self-service API `/api/v1/me`: profile, passkeys (with reauthentication rule), sessions, identities, grants; audit actor rules | TIO-ME-001..003, TIO-CONSENT-004, TIO-PK-040, TIO-PK-041 | ✅ DONE | 1c7af2a | `src/me/auth.ts` (`requireAccount`: user `at+jwt` with scope `account`, aud ∋ ISSUER, not a service client, not disabled; `ip_me` limit) and `src/me/handlers.ts` (15 routes, OpenAPI `ME_ROUTES` under the `accountToken` scheme). Registration challenges live on the user's object (`UserDO.putChallenge`/`takeChallenge`, `challenges` table = user schema version 2, keyed by the token's `sid` or `jti:<jti>` for offline tokens; single use; expired rows purged); `reauthentication_required` (403) when `auth_time` is older than `me.passkey_add_max_auth_age` on both options and registration; `passkeys.max_per_user` enforced; `created_via: me`. Sessions list `country`, `ua_family` and `current`; `DELETE /me/sessions` revokes every other session (`include_current=true` all) through `UserDO.revokeSessions`. Identities name the upstream alias and never the subject. Removals apply the last-method rule with 409 `last_login_method` (`removePasskey`/`removeIdentity` with actor `self`, P4-06). Email changes need `me.allow_email_change` and leave the address unverified, so `email_taken` cannot occur. `/me/events` answers 501 until P6-03. Test fault helpers gained `throwingDo` and `interceptDo` |
| P5-04 | E2E: logout with hint, logout confirmation without hint, self-service passkey add with `max_age=0` re-authorization | §13.5 e2e row | ✅ DONE | 1c7af2a | `test/e2e/logout.spec.ts` and `test/e2e/self-service.spec.ts` (green on Chromium and Firefox locally; CI runs Chromium). The example relying party gained `/logout` (hint, or `?nohint=1`), `/loggedout`, a security-settings page at `http://localhost:8788/me` that adds passkeys through the Self-service API and re-authorizes with `max_age=0` on `reauthentication_required`, `/me/passkeys*` proxies and `/handoff` (the loopback callback lives on 127.0.0.1, WebAuthn needs the `localhost` origin, cookies are per host). `scripts/e2e-server.ts` seeds `webauthn_origins` and `me.passkey_add_max_auth_age = 5`. The passkey provider gained `newDevice()` (a fresh CDP authenticator, since a registration that excludes the credentials an authenticator holds fails as WebAuthn requires). Shared flows in `test/e2e/support/flows.ts` |
| P5-05 | Phase 5 exit: trace shows §5.10, §8 covered | §14.2 | ✅ DONE | 1c7af2a | `scripts/trace.config.json` at phase 5: 231 covered, 0 errors; 437 tests, 100% coverage; e2e 10/10 on Chromium and Firefox. The `/api/v1/interactions/{id}/upstream/{alias}` route missing from the OpenAPI document since P4-02 was added here, and `test/http/openapi.test.ts` now also checks that every JSON API route of the table is documented |

---

## Phase 6 — Audit, observability, rate limits, cron

**Goal:** every security-relevant event is durable and queryable; abuse is bounded; the system cleans up after itself.

**Exit criteria:** §6.7, §11, §12.4 identifiers covered; soak test green.

| ID | Task | Spec | Status | Commit | Notes |
|---|---|---|---|---|---|
| P6-01 | Event catalog (`src/audit/catalog.ts`) with per-type data allow-lists, emitter, redaction; one test per event type; canary-string redaction test across every sink | TIO-AUDIT-001, TIO-AUDIT-002 | ✅ DONE | 6448201 | `src/audit/catalog.ts` (every §11.2 type plus `group.*`, each with its `data` allow-list; `target`/`diff` on Admin mutations), `src/audit/redact.ts` (values shaped like handles, JWTs, opaque secrets, addresses and user agents are replaced; addresses outside a `diff` are masked; keys named like secrets are replaced; `error_description` never ships), and the `Auditor` applies both and reports dropped keys at flush. Twenty-three types gained emitters (passkey ceremonies and clones, interactions, authorization, consent, tokens, sessions, keys from the cron, bootstrap, rate limits). `test/scripts/audit-catalog.test.ts` checks the catalog against the spec table, every emitter and every test; `test/http/audit-events.test.ts` triggers the flow events; `test/unit/audit-redaction.test.ts` feeds every type the canaries. Decision: key thumbprints travel as `kid:<thumbprint>` so the opaque-string rule needs no exemption. The user-level passkey attempt window of §6.7 landed here too (`UserDO.countPasskeyAttempt`, 10 per 10 min, 429 `rate_limited` with `Retry-After: 600`) |
| P6-02 | Queue producer batching in `waitUntil` and consumer writing `audit_hot` (`INSERT OR IGNORE`, batched) and gzip NDJSON to R2 with idempotent keys; ack only after both succeed; send failures never fail requests | TIO-AUDIT-010..012, TIO-DATA-025 | ✅ DONE | 6448201 | `src/audit/sink.ts`: `shipAuditEvents` (50 per message, after the response from the app, the cron and the consumer itself; a refused send is logged), `storeAuditBatch` (`INSERT OR IGNORE` in one D1 batch, 5 rows per statement because 17 columns × 5 stay under D1's 100 bound variables, then `audit/yyyy/mm/dd/hh/<first id>.ndjson.gz` on R2), and the `audit` branch of `src/queue/consumer.ts` (retry on any storage failure, drop what cannot be parsed). Migration `0007_audit_hot_columns.sql` adds `sid`, `interaction_id`, `country`, `ua_family`, `request_id`, `reason`. Note for tests: the vitest plugin delivers queued messages to the Worker's `queue()`, so `audit_hot` fills asynchronously during HTTP suites (assertions on it stay timing-independent) |
| P6-03 | Audit query endpoints (`/admin/audit`, `/admin/audit/archive`) and per-user views (`/me/events`, `/admin/users/{id}/events`) from `audit_hot` | §9.4 Audit, TIO-AUDIT-010 | ✅ DONE | 6448201 | `src/admin/audit-log.ts` and `listAuditPage` in `src/db/audit.ts`: newest first, keyset `(ts, id)` cursors bound to their listing, every §9.4 filter; the archive listing walks R2 by day prefix (at most 31 days, R2's own pages followed); the per-user views expose only the §8 fields within `audit.hot_retention_days`. The 501 placeholders are gone; OpenAPI documents all four |
| P6-04 | Retention: hot-table purge in cron (bounded iterations), invitation and retired-key cleanup already wired, `UserDO` purge windows verified | §4.7, TIO-CFG-010 | ✅ DONE | 6448201 | Already in place since P3-07/P3-10 and verified: `test/http/admin-system.test.ts` (purge of 1,000-row batches, bounded), `test/component/maintenance.test.ts` (budget, invitations, key retirement and deletion after 90 days with their events), `test/component/durable-objects.test.ts` (TIO-DATA-019 object purge windows), `test/http/scheduled.test.ts` |
| P6-05 | Rate limiting: `RL_IP` and `RL_CLIENT` bindings with the key table in §6.7, 429 handling, IPv6 /64 keys, fake binding in tests | TIO-RL-001, TIO-RL-003 | ✅ DONE | 6448201 | Every class of §6.7 is wired (`src/router/rate-limit.ts` `LIMIT_CLASSES`), `rateLimited(c, class)` now records `ratelimit.exceeded`, the IPv6 /64 key is cited by `test/http/par.test.ts`; the per-user passkey window landed with P6-01. Spec conflict kept from P3-03: §6.7 lists per-class limits while §12.1 declares two `simple` bindings, so the classes share a binding's limit (`RL_IP` 120/60 s, `RL_CLIENT` 2,000/10 s) with distinct key prefixes; exact per-class limits would need one binding per class, which wrangler allows but §12.1 does not list. Tests use the real local binding (rotating source addresses where a suite exceeds a minute's quota) |
| P6-06 | Metrics data points (requests and events) when `METRICS` is bound; health `degraded` on D1 failure | TIO-OBS-002, TIO-OBS-003 | ✅ DONE | 6448201 | One data point per request (since Phase 0) and one per audit event (`blobs: [type, outcome]`) in `src/router/app.ts`; health `degraded` since Phase 0. `test/http/router.test.ts` cites both |
| P6-07 | Soak scenario: 2 h refresh at 100/s on staging, p99 and per-user storage flat | TIO-TEST-052 | ✅ DONE | 0d30bc2 | Green on 2026-09-22 (run 35697861884, the fifth): two hours at 100/s, 720,000 refreshes, 0 failures, the 20 sampled exports within the retention bounds, warm p99 96 ms with the first ten minutes at 82 and the last at 102 (no growth), median 34 ms; the population re-created by the run's own runner first. Scenario written with P7-04. First real run on 2026-09-21/22 (35672252890): two hours at 100/s, 720,000 refreshes, 0 failures, per-user export sizes within the retention bounds, warm p99 172 ms (budget 200), first ten minutes 217 → last ten minutes 191 (no growth); the two windowed thresholds were still the pre-ADR-0016 150 ms literal and failed the run — they now read the row's budget and measure warm requests like the gate. The second run (35680103676) is void: dispatched at 02:36 UTC, it was still running when the 03:17 nightly deleted and re-imported the population under it, so 56 % of its refreshes belonged to deleted users and five sampled exports compared new objects with old ones (the first 52 minutes, before the delete, were clean: warm p99 83 ms, windows 108 and 42 ms). A manual soak must not overlap the nightly's load job — the two jobs now share a concurrency group. The third run failed its harvest on twelve logins that met a deploy (every push to main deploys staging; the harvest retries a login once on a server error now). The fourth run (35688731266) held every property again — 720,000 refreshes, 0 failures, storage within bounds — but at p50 94 ms / warm p99 474 against 23–40 / 83–172 before: it refreshed the population the nightly's runner had created in another region, so the soak job now deletes and re-imports the population from its own runner first, as the load job does. The fifth run is the green one |
| P6-08 | Phase 6 exit: trace shows §6.7, §11, §12.4 covered | §14.2 | ✅ DONE | 0d30bc2 | `scripts/trace.config.json` at phase 6: 239 covered, 0 errors (the one deferred id is TIO-TEST-052); 454 tests, 100% coverage. The last exit criterion, the soak on staging (P6-07), closed on 2026-09-22 |

---

## Phase 7 — Hardening and release

**Goal:** every identifier covered, conformance and load gates green, `v1.0.0` in production.

**Exit criteria:** trace reports zero uncovered ids; conformance results archived; load thresholds green; runbook and guide written; `v1.0.0` tagged and deployed.

| ID | Task | Spec | Status | Commit | Notes |
|---|---|---|---|---|---|
| P7-01 | Complete the security suite (every bullet in §13.7), including header and CORS matrices, enumeration equality, size limits, injection spies | TIO-TEST-020 | ✅ DONE | 90ae144 | Most bullets were already covered by the HTTP, component and unit suites (cited by requirement id there); `test/security/` adds what was missing: enumeration equality (token, userinfo, invitations, admin lookups incl. `users/{id}/events`, which now answers the same 404 as the other sub-resources), the D1 statement spy over every string parameter, the encoded-redirect and no-echo checks, the headers × routes matrix driven by the route table (every route gets a request, which is the TIO-TEST-008 route-coverage assertion), 413 on every body-accepting route and 414 on every navigation endpoint, JSON depth bombs, redirect_uri cases at `/authorize` and the unsteerable `login_url` handoff, a code_challenge shared by two clients, expired and cross-user code envelopes, and tampered / unknown-kid / retired-key access tokens at `/userinfo`, `/me` and `/admin`. Finding fixed on the way: the `client authentication failed` log line and the `token.client_auth_failed` event carried the presented `client_id` verbatim; only a well-formed id is reported now (malformed ids are refused without a lookup) |
| P7-02 | Complete the concurrency suite (remaining rows: federation state, verified-email user creation, identity link, bootstrap, first key) | TIO-TEST-010 | ✅ DONE | 764fbda | `test/concurrency/creation.test.ts` (first key on an empty store across 20 key stores, bootstrap, verified email, identity pair at creation) and `test/concurrency/federation.test.ts` (one `state` presented 20 times; 20 first logins of one upstream person). The bootstrap became an atomic claim (`claimSetting`: `INSERT … ON CONFLICT DO NOTHING`; the loser withdraws its invitation and answers 410) and `insertClient` lets the primary key decide. Finding fixed on the way: the callback's lazy release of an unconfirmed index row (TIO-DATA-026) also fired on a creation in flight (a `creating` holder whose object had not received the pair yet), so parallel first logins created several accounts for one upstream person; a `creating` holder is now a live claim (`identity_already_linked`, row kept) and the cron's repair of a `creating` row links the pairs the index claims for it instead of leaving them to be dropped later. `scripts/trace.config.json` at phase 7: 240 covered, 0 deferred (every test-verified id has a citing test); 478 tests, 100% coverage |
| P7-03 | Conformance: `conformance/` docker compose, plan configs (config, basic — which itself covers `client_secret_basic` and `client_secret_post` —, RP-initiated logout, back-channel logout), `waivers.json`, browser automation through the reference login app and the staging fake upstream; nightly job and archived results | TIO-TEST-040, TIO-TEST-041 | ✅ DONE | 83cc157 | Green on 2026-09-21 (run 35624305456): the four plans, 53 modules, 0 unexpected failures or warnings — 1 expected failure (RS256) and 5 expected warnings under the waivers, every plan exported to the `conformance-results` artifact (90 days; the release archives them, P7-06). Harness written; the plans were iterated to green on staging. `conformance/docker-compose.yml` runs the suite from its prebuilt images in dev mode; `conformance/run.ts` registers three relying parties on staging (`client`, `client2`, `client_secret_post`), rotates their secrets, renders the host-neutral templates of `conformance/plans/` and drives the suite's own `scripts/run-test-plan.py` (checked out sparsely in the job); `waivers.json` is validated (one permitted reason, the advertising discovery field) and turned into the suite's expected-failures file. The suite is exposed to the OP through a `cloudflared` quick tunnel because the back-channel logout plan needs the OP to reach the suite. The reference login app's controls gained stable ids for the automation (`upstream-<alias>`, `logout-confirm`, …). First runs (2026-09-20): the suite came up once `JAVA_EXTRA_ARGS` was dropped and its sign-in registrations got placeholders; the plan creation then refused every variant a plan fixes itself — the invocations now match the suite's own CI (`.gitlab-ci/run-tests.sh`: no variants for the config plan, `[server_metadata][client_registration]` for basic, `[response_type][client_registration]` for the logout plans; the basic plan runs both secret methods itself, so "three client-auth variants" was one plan, and no plan sends PKCE with `none`, which the interop suite covers instead). Spec conflict found by reading the suite's source before the next run: its modules send no `code_challenge` (only `oidcc-ensure-request-with-valid-pkce-succeeds` does), so TIO-AUTHZ-008 as written failed every login-dependent module, unwaivably. Decided by the owner (ADR 0013): a per-client `require_pkce` (default 1; a public client cannot clear it), the conformance parties registered with 0; `POST /authorize` added with it (OIDC Core §3.1.2.1; the suite's POST module warns, and an unexpected warning fails a plan). The suite's browser is HtmlUnit 4.17 (Spring Boot 4.0.7 BOM), read from the suite's source and the HtmlUnit change log before the next run: its Rhino engine parses no `async`/`await` (the whole script died, nothing rendered), has no `fetch` (an opt-in polyfill the suite does not enable) and no array spread before 4.19 — the reference app is rewritten with Promise chains, `XMLHttpRequest` where `fetch` is missing and no spread or rest (60c8970, ADR 0014, TIO-IX-080 extended); `test/scripts/login-app.test.ts` parses the app and refuses those constructs; the e2e suite signs in with `fetch` deleted. Verified under HtmlUnit by the runs of 2026-09-21 (dispatched with `only=conformance`): the rewritten app renders, the automation clicks `upstream-fake`, the XMLHttpRequest POST with the binding cookie passes CORS, the fake upstream approves and the OP's callback runs — the login then failed on policy, `registration_closed`: the fake's default person had never been created on staging and a first-time federated login is a registration (`registration.mode = invite`, no `federation.auto_create`, TIO-FED-040 rule 4). `run.ts` now provisions that person once with the upstream identity, as an operator imports federated users, so the login resolves by identity and staging's policy stays. Diagnostics that made this visible: the plans set `browser_verbose` and `run.ts` dumps every module's event log with a `.browser.txt` trace (an INTERRUPTED module leaves no export). Then, in order: intermittent `upstream_error` (`id_token:signature` in the audit log) — the fake upstream's object regenerated its keys on every restart under fixed kids, so an OP isolate with the cached JWKS rejected every signature and never refetched; the fake now keeps its keys in its object's storage under thumbprint kids (09d8841). The basic plan then ran to completion (33 of 35 modules; run 35617054405): waivers under the one permitted reason for the RS256 discovery failure (ES256 only, §1.5), the address/phone/all scope skips, the profile-claims and claims-parameter warnings; `request`/JAR `request_uri` refused as `request_not_supported`/`request_uri_not_supported` (TIO-AUTHZ-025, 7aa021e); the modules that ask a human to confirm a screen (prompt=login, max_age=1, the redirect-uri errors) stayed WAITING for an image placeholder, which the browser tasks now fulfil with the page; the RP-initiated logout plan's parties no longer register a `backchannel_logout_uri` (the suite serves that path in the back-channel plan alone); the quick tunnel is warmed before the plans. Two warnings remain that no permitted reason covers and that the spec chooses deliberately — `email` in the ID token for the `email` scope (TIO-TOKEN-030 vs OIDC Core §5.4's UserInfo placement) and the access token still valid at `/userinfo` after a code replay (TIO-TOKEN-012 revokes families, not the stateless 10-minute token) — decided by the owner: both behaviours stay, TIO-TEST-040 gains a second waiver reason for suite warnings on deliberate behaviour (ADR 0015, 83cc157). Run 35622944924 had every logout module green once the post-logout task stopped waiting for an element the suite's page lacks and the login app's default landing said `Signed out`; its one failure was the quick tunnel answering 502 on a callback, so the tunnel runs over HTTP/2 now and keeps its log in the artifact |
| P7-04 | Load: seed 1,000,000 users on staging via import (benchmark), harvest 100,000 refresh tokens over HTTP, k6 scenarios with §2.7 thresholds, D1 write-rate assertion | TIO-TEST-050, TIO-TEST-051, TIO-PERF-001, TIO-PERF-002, TIO-ADMIN-021 | ✅ DONE | fec80d3 | Green on 2026-09-21 (run 35669279646, full length, the nightly population of 1,000): all 38 thresholds — warm p99 session hit 123/200, refresh 96/200 (burst 92/300), exchange 99/200, userinfo 42/100, discovery 0/20, admin lists 863/1000, callback 994/1200; every overall p99 far under its 4× bound; import 28 users/s, harvest 1,000/1,000. The one-off 1,000,000-user import (TIO-ADMIN-021's benchmark, TIO-PERF-001 as written) is a cost decision of the owner's (OP-07); the harness runs it unchanged (`TIO_PERF_USERS`, a `bench` seed the nightly never deletes). Harness written. `perf/seed.ts` (`generate`, `prepare`, `import` with 8 clients and the sampled deep comparison, `harvest` = one federated login per user over HTTP through the fake upstream), `perf/scenarios/` (one k6 script per §2.7 row plus the soak; `budgets.js` is checked against the spec table by `test/scripts/perf-budgets.test.ts`), `scripts/load-summary.ts`, the nightly `load` job and the weekly `soak` job. The budgets are enforced on the new `Server-Timing` header (TIO-OBS-004: `app;dur`, `do`, `d1r`, `d1w`), which is what makes server-side budgets measurable from k6; the security suite now also asserts equal work across unknown/invalid input. Two spec conflicts found: (1) §6.7's per-address limit on `/token` (120/60 s) would throttle any server-side relying party and every load run from one runner — staging's `RL_IP` is raised to 100,000/60 s in `wrangler.jsonc` (production keeps the spec's value); decided by the owner: the address class on `/token` counts failed client authentication only (ADR 0012); (2) the 500/s refresh burst of §13.10 exceeds the 2,000/10 s per-client limit, so the harvest spreads users over three relying parties and the burst stays under 167/s each. `scripts/perf/seed.ts` moved to `perf/lib/lines.ts` (the spec's layout). First staging runs (2026-09-19 night, 1,000 users as the owner asked): `prepare` and the bootstrap work end to end; a 1,000-line import batch took ~130 s at ~7 users/s — every creation is two serialized D1 write round trips (~70 ms each from the Worker's colo), so eight parallel clients would not reach TIO-ADMIN-021's 278 users/s; the import needs cross-line batching of the D1 claims and activations (next step). The same batch exposed the Analytics Engine write limit (fixed, TIO-OBS-002). Creation now claims and activates a whole group in one D1 batch each and links identities inside `UserDO.init` (one object call per user instead of three): still ~8 users/s from São Paulo, because D1 sits in ENAM (~130 ms per round trip), the objects follow the first request's colo (~250 ms per call from here) and a Worker holds six outbound calls at a time — the runner in the US is where the benchmark is meaningful. First nightly run (1,000 users, 2026-09-19): prepare, import, harvest (1,000/1,000 at 50/s) and all seven k6 scenarios ran end to end with every check, failure-rate and D1-write threshold green; every server-side p99 budget failed, with p50s of 80–115 ms on each object-touching route — the objects of that population were created from São Paulo and the runner's traffic enters in the US, so the nightly now imports its own population (`TIO_PERF_SEED=nightly`) and the summaries carry p(99). Second nightly run (runner-created population): import 1,000 users in 31 s from one request (32 users/s), harvest at 1 s per login, discovery/userinfo/admin_list p99 within budget (17/58/116 ms), refresh p50 33 ms against a 30 ms budget but p99 238 ms (burst 1,052 ms) — the isolate caches now refresh early in the background (82c37b9); three scenarios had not run because of a broken import list, now syntax-checked by a unit test. Third nightly run (2026-09-20, all seven scenarios; the run-2 population reused through `--skip-if-seeded`): every check, failure-rate and D1-write threshold green except `login_federated` checks at 0.84; p99 over budget on sso_authorize (210 vs 150), login_federated (1,214 vs 800), admin_list (503 vs 300), refresh (175, burst 336 vs 150); the medians of the object-touching routes were ~80 ms against ~33 ms the night before with faster code — the objects had been placed by the previous night's runner and this runner entered Cloudflare elsewhere, so every object call crossed colos. The nightly now deletes and re-imports its population every night (`seed.ts delete`, c4c118a) so the objects follow the runner, and the summaries split the server-side duration into warm and D1-touching requests (`server_ms_warm`/`server_ms_d1`) to tell a cold isolate's loads from the object round trip in the tail (p95 ≈ 90 ms, p99 ≈ 175–336 ms). The 2026-09-21 scheduled run and the 2026-09-20 weekly soak both failed at their first request because Workers Builds had deployed a misconfigured Worker (see OP-01); the soak's harvest spent 800 s failing all 40,000 logins, so the harvest now stops after the first 100 consecutive failures (302c6af). Runs four to seven (2026-09-21, population re-placed by the runner each time) settled the picture: the requests that read no D1 met every p99 budget (sso 116, refresh 81, exchange 79, userinfo 24, discovery 22 ms); the p99 over all requests was set by the 2–8 % of requests reading D1 synchronously — cold isolates loading settings, keys and the client in three sequential ENAM round trips — and by the D1-inherent rows (admin lists 930, callback 889 ms). Decided by the owner (ADR 0016): budgets apply to warm requests, the D1-touching tail is bounded at 4× the budget, the callback and admin-list rows carry measured budgets (600/1200, 300/1000). Code on the way: upstream records cached per isolate (the callback's second D1 read, e5e3436), settings and keys loaded together and shared by concurrent requests on a cold isolate, a thrown object init repeated once, the seed's delete and lookups tolerant of a 503 or a reset connection (each of which had ended a run). Conformance: the suite came up once `JAVA_EXTRA_ARGS` was dropped (JVM options, not Spring args) and the suite's own sign-in registrations got placeholder values |
| P7-05 | Documents: `doc/RUNBOOK.md` (bootstrap, key rotation, master-key rotation, emergency retirement, secret rotation, recovery, D1 restore, reindex, lost `MASTER_KEYS`), `doc/LOGIN_APP_GUIDE.md`, `doc/adr/` for decisions made during the build, threat-model review sign-off | TIO-DEPLOY-004, TIO-SEC-001, TIO-GEN-005 | ✅ DONE | 8bea435 | Written: `doc/RUNBOOK.md` (thirteen procedures, each naming its test or saying it has none; the lost-`MASTER_KEYS` path retires the unopenable keys in D1 because no admin token can be minted without a signing key), `doc/LOGIN_APP_GUIDE.md` rewritten for every state of §7 (federation, linking, consent, logout, the error table), `doc/FEDERATION_SETUP.md` (connecting upstream providers: the generic procedure and the record's fields, Google and Microsoft Entra ID single-tenant step by step with their claim quirks, the account-resolution settings, verification through the Admin API, troubleshooting), `doc/adr/0001`–`0010` for the decisions recorded in this plan's notes since Phase 0, and `doc/adr/0011` the Phase 7 threat-model review (evidence table generated from the traceability, six findings absorbed, one recommendation: a `user.exported` event). The D1 restore procedure gained its drill test in `test/http/admin-import.test.ts` (import a line with the id, reindex). The review's one recommendation, a `user.exported` event for the only admin read that hands over a whole record, was accepted by the owner and added (§11.2 row, catalog, emitter, test). 2026-09-22: the owner had ADR 0011 reviewed before signing (`tmp/adr-0011-review-handoff.md`, verified against the tree); its work items are done — §15 rows T1 and T14 state the rules of ADR 0013 and ADR 0012, `/login/*` is in the §5.1 endpoint map (it existed but TIO-HTTP-001 said every unlisted path is 404), the evidence table is derived by `pnpm trace` (the "Threat evidence" section of `doc/TRACEABILITY.md`; T1 and T21 rows corrected, T8's six unassigned ids named), the wording corrections (lint rule name, the per-file interceptor and the test T23 rests on, finding 4's two limits, the `user.exported` acceptance date), and a dated addendum in ADR 0011 for the post-review surface changes and the closed open items. The second review's last item was decided by the owner on 2026-09-22: no table-drift test (it fires on ordinary work and would be cleared by editing a signed record), but a surface snapshot instead — `test/scripts/threat-surface.test.ts` [TIO-SEC-001] over the route table, the handle kinds and every §15 row's requirement ids, accepted with `pnpm surface --write` in the commit that reviews the change; the record's table now carries its commit and date and points at the generated section as the living view. Signed by the owner on 2026-09-22: ADR 0011 is Accepted, the sign-off row filled and the index updated. `doc/FEDERATION_SETUP.md` was added the same day for the operator side of §6.4 |
| P7-06 | Release: create the `production` branch, Workers Builds production Worker (OP-02), `deploy.ts` versions upload + smoke + deploy exercised, `v1.0.0` tag, README status updated | TIO-DEPLOY-006, TIO-DEPLOY-010 | 🔧 IN PROGRESS | — | Release rehearsal against the staging Worker on 2026-09-22 ([ADR 0018](adr/0018-staged-rollout-through-version-overrides.md)): the production path of `deploy.ts` could never have succeeded — `wrangler versions upload` prints no preview URL for this Worker (the script required one; its fake wrangler printed one the real one never did), and a preview host answers 421 on discovery and JWKS (TIO-HTTP-006). Replaced by a staged rollout that staging and production share, so every push to `main` rehearses the release: the live version is read before anything changes, the new version joins the deployment at 0%, the smoke test reaches it on the issuer's hostname through `Cloudflare-Workers-Version-Overrides` and checks health's `VERSION`, then 100% — or the live version is restored and the build fails. Run for real on staging: a sabotaged smoke test restored the live version (38 s, live build unchanged); a clean rollout took six wrangler commands in 38 s. `TIO_DIRECT_DEPLOY=true` covers the first production deployment and Durable Object class migrations (runbook §13). TIO-DEPLOY-007 amended. Remaining: OP-02 (on hold), the `production` branch and its protection, the first production deployment, `v1.0.0`, the README status |
| P7-07 | Mutation-testing baseline in nightly (report only) | §13.1 | ✅ DONE | e1eaf82 | Non-blocking by decision. Stryker 10 with the Vitest runner (`stryker.config.json`, `pnpm mutate`); the runner pins `pool: threads`, which displaces the Cloudflare pool, so only the Node `unit` project takes part until the runner and the plugin agree (the caveat §13.1 anticipated). Baseline on this workstation: 12,330 mutants, 1,482 detected of 1,620 covered (91.48% covered score), 10,710 without unit coverage (the workerd-only code: `do`, `db`, `interaction`, `me`, `logout`, …), 0 errors, 2 min 34 s. The nightly job uploads `reports/mutation/` and writes the score table to the job summary through `scripts/mutation-summary.ts` (metrics tested in `test/scripts/mutation-summary.test.ts`) |
| P7-08 | Phase 7 exit: zero uncovered ids, all nightly gates green, production live | §14.2 | ❌ OPEN | — | |

---

## Operator tasks (repository owner)

| ID | Task | When | Status | Commit | Notes |
|---|---|---|---|---|---|
| OP-01 | Connect the repository to Cloudflare Workers Builds for staging: Worker `tiny-oidc-staging`, branch `main`, build `pnpm run build`, deploy `pnpm run deploy`, build variables `TIO_ENV`, `TIO_ISSUER`, `TIO_RP_ID`, `TIO_RP_NAME` | After P0-14 | ✅ DONE | — | Values are in the private notes, never in this repo. Requested from the owner on 2026-09-19 (mobile notification) after Phase 0 went green; not blocking Phase 1. 2026-09-19 evening: the staging Worker, its D1 database, queues, R2 bucket and secrets were created and the first deploy made from the workstation (`pnpm run deploy` with the staging values), so the Worker exists for the connection; what remains for the owner is connecting the repository in the dashboard (GitHub app) with the four build variables. Connected on 2026-09-20, but the `TIO_ISSUER` build variable holds the literal text `TIO_ISSUER`, so every push deployed a Worker that answered every request with 500 `server misconfigured` (the 2026-09-21 scheduled nightly failed on it at its first request). Until the owner corrects the value in the dashboard, `deploy.ts` refuses unusable variables before any wrangler command (96a5a09), so a push now fails the Workers Build and leaves the last workstation deploy live; after each push the staging Worker is redeployed from the workstation with the private values. Finding on the way: `wrangler` does not inherit `durable_objects`, `ratelimits` and `analytics_engine_datasets` into environments, so the first staging deploy had no Durable Object bindings — fixed in 253460c with a per-profile config check Done by the owner on 2026-09-22: the `TIO_ISSUER` build variable corrected; staging has deployed every push since. |
| OP-02 | Connect Workers Builds for production: Worker `tiny-oidc`, branch `production`, same commands, production build variables | Before P7-06 | ❌ OPEN | — | |
| OP-03 | Review and merge the infrastructure-repository change attaching the five custom hostnames to the staging and production Workers | After OP-01 and the staging fixtures exist (P4-07) | ✅ DONE | — | PR #5 opened by the implementing agent in the private infrastructure repository on 2026-09-19: the three staging hostnames (OP, login app, fake upstream) plus the login-app deploy harness (a static-assets Worker per environment that sets the `tio-issuer` meta tag and a `_headers` file); the plan is 3 to add, 0 to change. The production pair follows with P7-06 Merged on 2026-09-20; the three staging hostnames are live. The production pair is part of P7-06. |
| OP-04 | Create a Google OAuth client and a Microsoft Entra app registration for staging with the staging callback URL; hand over ids and secrets outside the repo | Before P4-08 | 🔧 IN PROGRESS | — | Google's client handed over on 2026-09-22 (private notes) and used; the Microsoft registration remains, tracked as [issue #2](https://github.com/fdcastel/tiny-oidc/issues/2) with `doc/FEDERATION_SETUP.md` §3 as its instructions |
| OP-05 | Confirm or change the assumed defaults: no DO jurisdiction, `registration.mode` `open` on staging and `invite` on production, metrics enabled, no R2 lifecycle rule | Any time before P7-06 | ❌ OPEN | — | Defaults are applied until changed |
| OP-07 | Decide the one-off 1,000,000-user benchmark on staging (TIO-ADMIN-021, TIO-PERF-001 as written): rough cost of the import ≈ 4–5 M D1 row writes and 1 M object creations (a few tens of dollars once) plus ≈ 5 GB of object storage while it exists (≈ $1/month); the nightly keeps its 1,000-user population, which it deletes and re-imports every night, so the benchmark population would carry its own seed and stay | After P7-04 | ❌ OPEN | — | The owner chose 1,000 users first (cost check) on 2026-09-19; the gate is green at that population |
| OP-06 | Deploy the fake upstream to staging: `TIO_ENV=staging TIO_FAKE_ISSUER=<fake issuer> TIO_FAKE_CLIENT_ID=<id> TIO_FAKE_CLIENT_SECRET=<secret> TIO_FAKE_REDIRECT_URIS=<staging issuer>/federation/callback pnpm run deploy:fake-upstream` from a workstation with a wrangler login (or authorize the implementing agent to run it), then register it as an upstream on staging | After OP-01 | ✅ DONE | — | The values are in the private notes; the fake's hostname is attached in the infra repository together with OP-03. Requested from the owner on 2026-09-19. Deployed from the workstation on 2026-09-19 (evening) with the staging values; the registration as an upstream waits for the hostnames (OP-03) and the staging bootstrap Registered on staging as upstream `fake` on 2026-09-20 by the load harness's prepare step; every conformance and load run since has logged in through it. |

---

## Backlog (deferred beyond v1)

| ID | Item | Spec | Status | Commit | Notes |
|---|---|---|---|---|---|
| B-01 | DPoP sender-constrained tokens (RFC 9449) | Appendix B | ⏯️ DEFERRED | — | Re-evaluate when a resource server requires sender constraining |
| B-02 | Apple Sign-in upstream (JWT client secret, cross-site `form_post` callback) | Appendix B | ⏯️ DEFERRED | — | Blocked by the `SameSite=Lax` binding cookie model |
| B-03 | RFC 8707 resource indicators (per-request audience) | Appendix B #29 | ⏯️ DEFERRED | — | Static `audiences` covers v1 |
| B-04 | Self-service identity linking (`link` interaction, `/me/identities/link`) | Appendix B #30 | ⏯️ DEFERRED | — | Login-time linking covers v1 |
| B-05 | Configuration as code (`PUT /admin/config`) | Appendix B #33 | ⏯️ DEFERRED | — | CRUD + `client_credentials` covers automation |
| B-06 | `jti` replay cache for `private_key_jwt` | Appendix B #28 | ⏯️ DEFERRED | — | Only if a profile such as FAPI is targeted |
| B-07 | EdDSA as a signing option; pairwise subject identifiers; device authorization grant; token exchange; outbound webhooks; SCIM server; D1 directory sharding beyond ~5,000,000 users | §1.5 | ⏯️ DEFERRED | — | Post-1.0 candidates |

---

## Plan change log

| Date | Change |
|---|---|
| 2026-09-19 | Plan created from spec §14.2 after the reference review; Phase S recorded as done; all implementation tasks `❌ OPEN`. |
| 2026-09-19 | Recorded the plan's own commit hash in S-04. |
| 2026-09-19 | P0-01 and P0-02 implemented (scaffold, toolchain, migration 0001, `resetStorage`); marked in progress until CI (P0-03) runs them. |
| 2026-09-19 | P0-04 (trace tool, phase allow-list, `doc/TRACEABILITY.md`) and P0-05 (lint rules, ignore count, config, neutrality and bundle checks) implemented with unit tests; in progress until CI runs them. |
| 2026-09-19 | P0-03 (workflows), P0-06 (env, config, settings loader), P0-07 (crypto: random, hash, master keys, envelope, uuid) implemented with tests; `test/support/clock.ts` and `keys.ts` from P0-13. |
| 2026-09-19 | P0-08 (D1 wrapper, settings repository) and P0-09 (UserDO schema, guards, purge; InteractionDO document, alarm, transitions) implemented with tests. |
| 2026-09-19 | P0-10 (router skeleton: request id, startup config, host check, limits, security headers, CORS, JSON errors, log line, metrics, health, OpenAPI) implemented with tests. |
| 2026-09-19 | P0-11 (generated `doc/CONFIG.md`, `.dev.vars.example`, `doc/openapi.json`, `gen-secrets`) implemented with tests. |
| 2026-09-19 | P0-12 (deploy and smoke scripts with a fake wrangler) and P0-13 (clock, keys, factories, outbound guard) implemented. Spec draft.2: `fetchMock` replaced by `@msw/cloudflare` in TIO-ARCH-016, TIO-TEST-031 and §13.1; dev dependency list in §14.1 extended. |
| 2026-09-19 | Phase 0 complete: P0-01..P0-14 done; e2e smoke suite with Playwright `webServer` starting `wrangler dev`; README status updated. Chromium for Playwright had to be installed by hand on the workstation (Node's downloader times out against the CDN; curl works). |
| 2026-09-19 | First CI run green on `main` (e561660). OP-01 requested from the owner. |
| 2026-09-19 | Phase 1 complete (P1-01..P1-07): key store, roles, rotation, JWKS, discovery, capabilities, JWT service, token builders, master-key rotation. Spec §2.8 key cache aligned to 60 s per TIO-ARCH-011. Smoke test now checks discovery, JWKS and health. |
| 2026-09-19 | P2-01 client model: TIO-DATA-003 reworded (generated client ids `c_` + 22 chars of `[a-z0-9]`, since base64url contradicts the lowercase pattern). |
| 2026-09-19 | P2-08 UserDO core: sessions, codes, refresh families with reuse detection, grants, passkey and identity storage. Spec §4.2 amended with `refresh_families.code_secret_hash` (TIO-TOKEN-012). |
| 2026-09-19 | P2-06 passkey ceremonies, virtual authenticator and `UserDO.verifyAssertion`; P2-01 and P2-08 commit hashes recorded. Plan title line restored (a scripted edit had mangled it since 75ff0c2; rows were intact). |
| 2026-09-19 | P2-02 client authentication (four methods, `private_key_jwt` against `jwks`/`jwks_uri`) with 11 component tests. |
| 2026-09-19 | P2-03 `/authorize`: validation pipeline, session evaluation, interaction start, PAR consumption. `InteractionDO` sections typed concretely (§4.3) so documents cross the RPC boundary; `par_consumed` flag and `claimPushed` added for single-use `request_uri`. |
| 2026-09-19 | P2-04 `/par` with the shared form/client-auth helpers and the first rate-limit keys; §6.7 vs §12.1 rate-limit binding conflict recorded for P6-05. |
| 2026-09-19 | P2-05 Interaction API guard, document, consent and abort (14 HTTP tests); `test/support/sessions.ts` shared session fixture. |
| 2026-09-19 | P2-06 passkey sign-in on the Interaction API (options, verify, routing, attempts, access_denied outcomes) with the user-creation and passkey-registration services (§4.6) and the `users`, `passkey_index` and `groups` repositories. TIO-IX-011 and TIO-IX-070 of P2-05 covered. |
| 2026-09-19 | P2-09 `/interactions/{id}/complete` with 9 HTTP tests; full passkey login flow now runs end to end through HTTP (authorize → interaction → passkey → complete → code exchange in the DO). |
| 2026-09-19 | P2-10 `/token` (three grants, tokens verified against the served JWKS in tests); admin-scope membership enforced at every point of TIO-SCOPE-002. |
| 2026-09-19 | P2-11 `/userinfo` and `/revoke` with 5 HTTP tests. |
| 2026-09-19 | P2-07 registration policy, invitations and the `register/options` + `register/verify` endpoints (9 HTTP tests incl. the invitation race). P2-06 TIO-PK-041 (names) done here. |
| 2026-09-19 | P2-12 groups service and repository rules; P2-13 bootstrap endpoint (6 HTTP tests incl. the full bootstrap→registration→admin token flow). Workers test timeout raised to 30 s because rate-limit tests exhaust a binding under parallel load. |
| 2026-09-19 | P2-14 reference login app, `/login/*` serving and `doc/LOGIN_APP_GUIDE.md`. |
| 2026-09-19 | P2-15 interop (oauth4webapi) and e2e (Playwright, openid-client RP, real browsers with passkeys) suites; `test/support/virtual-authenticator.ts` types made portable to the Node type set so the e2e suite can reuse it. |
| 2026-09-19 | P2-16 concurrency suite over HTTP (6 races). `InteractionDO` writes serialized with `blockConcurrencyWhile`; `/complete` claims the interaction before minting the code. Conflict recorded on the row: 20 parallel passkey verifications vs the 10-attempt limit. |
| 2026-09-19 | Phase 2 exit (P2-17): trace phase 2, seven uncited identifiers given tests, TIO-CLIENT-004 lazy family revocation, P2-01/P2-05/P2-06/P2-08 closed. Phase 3 starts while the staging nightly waits on OP-01 (independent of it). |
| 2026-09-19 | P3-01 admin authorization, audit emitter and diff, keyset cursors, `GET /admin/users`; the Admin API guard runs for every path under the prefix except bootstrap. |
| 2026-09-19 | P3-02 users endpoints (create, get, patch, disable/enable, delete, passkeys, identities, sessions, refresh families, grants, recovery invitations, reindex, export, restore). Vanishing-object tests use a per-user sabotaging `USER_DO` proxy (`test/http/admin-users.test.ts`). |
| 2026-09-19 | P3-03 groups endpoints with propagated renames/deletions and dual-write membership; admin rate-limit key changed to the full `jti`; `group.*` audit events added ahead of the P6-01 catalog. |
| 2026-09-19 | P3-04 clients endpoints (CRUD, rotate-secret, disable/enable, lazy cleanup on delete). |
| 2026-09-19 | P3-05 upstreams endpoints with discovery validation, sealed secrets and the test endpoint. |
| 2026-09-19 | P3-06 invitations endpoints (create, list, get, revoke). |
| 2026-09-19 | P3-07 keys, settings, stats and maintenance endpoints; the cron body (`src/maintenance/run.ts`) built here for P3-10 to schedule. |
| 2026-09-19 | P3-10 `scheduled()` wired over the maintenance body, done ahead of P3-08/P3-09 because the body already existed. |
| 2026-09-19 | P3-08 bulk import and the seed generator. |
| 2026-09-19 | Phase 3 exit (P3-11): trace phase 3; admin reads bypass caches (TIO-ARCH-013); stored-data column pin (TIO-PRIV-001); P3-09 closed. |
| 2026-09-19 | Recorded c8269b4 for P4-01..P4-07 and P4-09. |
| 2026-09-19 | Recorded 1c7af2a for P5-01..P5-05. |
| 2026-09-19 | Recorded 6448201 for P6-01..P6-06 and P6-08. |
| 2026-09-19 | P6-01..P6-06: the audit catalog with allow-lists and redaction, twenty-three new emitters, the queue sink (producer, `audit_hot` + R2 consumer), the audit endpoints, retention verified, rate-limit events and the per-user passkey window, per-event metrics. P6-07 deferred on OP-01; P6-08 in progress on it; trace at phase 6. |
| 2026-09-19 | P5-01..P5-05: RP-initiated logout, the logout interaction, back-channel logout with the queue consumer and retries, the Self-service API, the e2e logout and self-service specs; trace at phase 5. Loopback port tolerance applied to `post_logout_redirect_uri` (TIO-LOGOUT-002 vs TIO-CLIENT-011, noted in P5-01). |
| 2026-09-19 | Recorded 90ae144 for P7-01. |
| 2026-09-19 | Recorded 764fbda for P7-02. |
| 2026-09-19 | Recorded 8bea435 for P7-05 (still in progress on the sign-off) and e1eaf82 for P7-07. |
| 2026-09-22 | P7-06 started with a release rehearsal on staging (ADR 0018): the production deploy path could not have worked (no preview URL from `wrangler versions upload`; 421 on a preview host), and is replaced by a staged rollout — new version at 0%, smoke test on the issuer through the version-override header, then 100% or the live version restored — used by staging on every push. Both outcomes run for real on staging. TIO-DEPLOY-007 amended; runbook §13 gains the first-deployment and Durable Object migration procedures. |
| 2026-09-22 | P7-05 done: the owner signed ADR 0011 (Accepted; sign-off row and index updated). The Microsoft half of P4-08/OP-04 moved to [issue #2](https://github.com/fdcastel/tiny-oidc/issues/2), with `doc/FEDERATION_SETUP.md` §3 as the instructions. |
| 2026-09-22 | The second ADR 0011 review's last item decided by the owner: the proposed drift test between the record's evidence table and the generated `Threat evidence` section was declined (it moves whenever a test changes a citation, and clearing it would mean editing a signed record), and replaced by a surface snapshot: `test/scripts/threat-surface.test.ts` [TIO-SEC-001] holds the route table, the handle kinds and every §15 row's requirement ids against `test/scripts/threat-surface.json` (`pnpm surface`, `--write` to accept after a review). The handle table moved to `src/crypto/handle-types.ts` so Node-side tooling can read it. The record's table now carries its commit and date, and says the generated section is the living view. |
| 2026-09-22 | `doc/FEDERATION_SETUP.md` added (P7-05): the operator's guide to connecting upstream providers — the generic procedure and record fields, Google and Microsoft Entra ID (single tenant) step by step, the account-resolution settings, verification through the Admin API and a troubleshooting table. Linked from the README. |
| 2026-09-22 | Google's third pass on staging (ADR 0017): `prompt=login` re-authenticated through Google, resolved the existing account by TIO-FED-040 step 1 and rotated the session in place (TIO-SESS-002); Google is done, Microsoft is the rest of P4-08. |
| 2026-09-22 | Google's second pass on staging (ADR 0017): the existing session served a second authorization with no interaction created (TIO-AUTHZ-014, TIO-AUTHZ-023), and a second Google account of the owner's auto-created its own staging account (TIO-FED-040 step 3). |
| 2026-09-22 | P2-17, OP-01, OP-03 and OP-06 closed on the day's facts (Workers Builds deploying every push, the hostnames live, the fake upstream registered). |
| 2026-09-22 | P6-07 done: the soak green on its fifth run (the first proved the properties; the second collided with the nightly, the third with a deploy, the fourth measured another region's objects — each now prevented by the job itself); P6-08 closed with it. |
| 2026-09-22 | Google verified on staging (P4-08, ADR 0017): a real login by the owner through the example relying party; Microsoft pending. |
| 2026-09-22 | ADR 0011 brought up to date for signing (P7-05): §15 T1/T14, §5.1 `/login/*` row, `pnpm trace` derives the threat evidence, addendum. Google registered on staging (P4-08, OP-04 partial). |
| 2026-09-22 | First soak run (P6-07): every property of TIO-TEST-052 held over two hours; the windowed thresholds read the row's budget now. |
| 2026-09-21 | P7-04 done: the load gate green on a full-length run (all 38 thresholds). On the way: the tail bound over every request (the D1-touching subset's p99 swung twofold on identical code), `sso_authorize` over a 200-session slice (object cold starts), the public documents held in isolate memory (the Worker cache match spent discovery's budget), the burst at 1.5× and the two object-write rows at 200 ms (measured 135–156). OP-07 added for the 1M benchmark; P6-07 (soak) dispatched. A `duration` dispatch input shortens iteration runs to ~12 min. |
| 2026-09-21 | ADR 0016 (owner's decision): §2.7 budgets on warm requests, the D1-touching tail bounded at 4×, callback and admin-list rows measured; TIO-PERF-001 amended. Cold isolates load settings and keys in one parallel round trip shared by concurrent requests; upstream records cached (§2.8 row). |
| 2026-09-21 | P7-03 done: the conformance gate green on staging (run 35624305456). Two CI fixes on the way: the workers project has no Node types (`Buffer` in a test), and the self-service e2e read the passkey list before the page refreshed it. |
| 2026-09-21 | ADR 0015: a second waiver reason (suite warnings on behaviour the spec chooses deliberately; owner's decision) — TIO-TEST-040 amended; the login app's `?event=logged_out` landing says `Signed out`; the four plans run to completion and export. |
| 2026-09-21 | Conformance iterated to the end of the basic plan: fake-upstream keys persisted (09d8841), waivers, TIO-AUTHZ-025 (`request_not_supported`), image placeholders, per-plan relying parties, tunnel warm-up; the fake clock moved to 2033 after real time crossed its base (845e382). |
| 2026-09-21 | Conformance: the first end-to-end traces under HtmlUnit (browser_verbose, module logs dumped by `run.ts`) showed the automation working and the login refused as a registration; `run.ts` provisions the fake upstream's person on staging. Harvest fail-fast after 100 consecutive failed logins. |
| 2026-09-21 | `deploy.ts` validates `TIO_ISSUER`, `TIO_RP_ID` and `TIO_RP_NAME` before any wrangler command (TIO-DEPLOY-007): the Workers Builds `TIO_ISSUER` build variable holds its own name and had replaced the working staging Worker with a misconfigured one; the scheduled nightly of 2026-09-21 failed on it. The reference login app rewritten for the conformance suite's browser (HtmlUnit 4.17: no `async`/`await`, `fetch` or spread) with a parsing test that keeps it so, ADR 0014, TIO-IX-080 extended, spec date 2026-09-21. An e2e test that signed in twice without carrying the authenticator's signature counter to the provider made the following test's login a counter regression; fixed in the test. |
| 2026-09-20 | Third nightly run recorded in P7-04: object placement follows the runner that created the population, so the nightly deletes and re-imports its population every night; `server_ms` split into warm and D1-touching requests in the summaries; the nightly gained an `only` dispatch input to iterate on one gate. |
| 2026-09-21 | Spec: TIO-AUTHZ-025 (a `request` parameter is `request_not_supported`), TIO-AUTHZ-003 (a JAR `request_uri` is `request_uri_not_supported`), TIO-IX-080 (the reference app runs under HtmlUnit). |
| 2026-09-20 | Spec: PKCE required by default with a per-client `require_pkce` clearable by confidential clients only (§1.5, TIO-AUTHZ-008, TIO-TOKEN-011, TIO-CLIENT-002, the `clients` and `auth_codes` tables, Appendix B #37, ADR 0013) — the conformance suite's modules send no `code_challenge`; `POST /authorize` (TIO-AUTHZ-001, route table); TIO-TEST-040 reworded to the plans as the suite defines them (the basic plan covers both secret methods, `none` is the interop suite's). D1 migration 0008, `UserDO` schema version 3 (nullable challenge, table rebuilt with its rows). Conformance harness: four runs with the selectable variants only, three relying parties, the `client_secret_post` block. |
| 2026-09-20 | Isolate caches (settings, keys, clients) refresh early in the background from three quarters of their TTL (TIO-ARCH-011 wording extended; `src/util/swr.ts`), after the second nightly run put the refresh p99 at 238 ms against a 33 ms median. k6 scenario scripts are syntax-checked by the unit suite after a broken import list silently skipped three scenarios. |
| 2026-09-19 | Batched user creation (`createUsers`: one claim batch, parallel `init` with the identities linked inside it, one activation batch) behind the import and `createUser`; the first nightly run's numbers and the object-placement lesson recorded in P7-04; the conformance compose fixed. |
| 2026-09-19 | First staging measurements (P7-04): a 1,000-line import emitted 3,000 audit events in one request and hit the Analytics Engine per-invocation write limit, turning a finished import into a 500 — TIO-OBS-002 now aggregates event points per type and outcome with a count and a refused write never fails a request. Throughput measured at 7 users/s (D1 write round trips serialized per user; see the P7-04 row). |
| 2026-09-19 | Spec §6.7: the per-address class on `/token`, `/par`, `/revoke` counts failed client authentication only (owner's decision on the P7-04 conflict; ADR 0012). |
| 2026-09-19 | P7-03 harness: `conformance/` (compose, plan templates, browser automation, waivers, `run.ts`), the nightly `conformance` job with a cloudflared quick tunnel; login-app control ids. |
| 2026-09-19 | P7-04 harness: `perf/` (seed, harvest, k6 scenarios, budgets), the nightly `load` and weekly `soak` jobs, `scripts/load-summary.ts`. Spec gained TIO-OBS-004 (`Server-Timing` with the request's server-side duration and DO/D1 counts) so the §2.7 budgets are enforceable from k6; two §6.7 conflicts recorded in the row. |
| 2026-09-19 | Staging brought up from the workstation (OP-01 groundwork, OP-06 deploy, OP-03 PR): the three Workers exist; `wrangler.jsonc` environments gained the non-inherited bindings (253460c). |
| 2026-09-19 | Spec §11.2 gained `user.exported` (the export is the one audited admin read), decided in the threat-model review (ADR 0011) and accepted by the owner; catalog, emitter and test in the same commit. |
| 2026-09-19 | P7-07 mutation baseline: Stryker over the unit project in the nightly, report only; `scripts/mutation-summary.ts`. |
| 2026-09-19 | P7-05 documents: runbook, login-app guide rewritten, `doc/adr/` with ten decision records and the threat-model review (0011, awaiting sign-off); D1 restore drill test. |
| 2026-09-19 | P7-02 concurrency suite: `test/concurrency/{creation,federation}.test.ts`, the atomic bootstrap claim, the `creating`-holder rule in the federation callback and the identity re-link in the cron repair; trace at phase 7. |
| 2026-09-19 | P7-01 security suite: seven files under `test/security/` (enumeration, injection, redaction, headers, limits, redirects, tokens), the `client_id` reporting fix in client authentication, `users/{id}/events` aligned with the other user sub-resources. |
| 2026-09-19 | P4-01..P4-07 federation: metadata cache, outbound leg, callback, ID-token rules, account resolution, the last-login-method rule (spec conflict TIO-FED-051 vs §9.4 decided for the user, not the admin), fake upstream with its staging Worker and deploy refusal, outbound allow-list test. P4-08 deferred to OP-04; P4-09 in progress on that criterion; OP-06 added (fake upstream staging deployment); trace at phase 4. |
